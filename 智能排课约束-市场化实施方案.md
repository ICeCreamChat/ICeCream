# 智能排课约束 — 市场化完整实施方案

> **版本**：v1.0（2026-07-08）
> **一句话目标**：真实理解用户说的排课自然语言（几乎所有），成功转化为机器规则，应用到真实的排课求解中去，并把结果讲清楚。
> **本文档用途**：交给实施者照着做的完整施工手册。每个任务都写明改哪个文件、加什么函数、怎么验收。不需要实施者自己做设计决策。
> **写作依据**：本文所有"现状"描述都逐一核对过仓库代码，关键事实标注了 `文件:行号`。实施前如发现行号漂移，以函数名搜索为准。

---

## 目录

- [0. 阅读指南与全局约定](#0-阅读指南与全局约定)
- [1. 现状全景（必读）](#1-现状全景必读)
- [2. 三层架构与约束类型总目录](#2-三层架构与约束类型总目录)
- [3. Phase 0 — 信任修复：把解析结果讲清楚](#3-phase-0--信任修复把解析结果讲清楚)
- [4. Phase 1 — 规则底座扩容（9 种 → 22+ 种）](#4-phase-1--规则底座扩容9-种--22-种)
- [5. Phase 2 — AI 语义理解引擎（AI-first）](#5-phase-2--ai-语义理解引擎ai-first)
- [6. Phase 3 — 多轮澄清对话](#6-phase-3--多轮澄清对话)
- [7. Phase 4 — 求解反馈闭环](#7-phase-4--求解反馈闭环)
- [8. Phase 5 — 端到端 AI Agent](#8-phase-5--端到端-ai-agent)
- [9. Phase 6 — 市场化硬化](#9-phase-6--市场化硬化)
- [10. UI 设计总规范](#10-ui-设计总规范)
- [11. 测试与验收总表](#11-测试与验收总表)
- [12. 实施顺序、依赖与风险](#12-实施顺序依赖与风险)
- [附录 A：约束类型 ↔ 代码位置速查表](#附录-a约束类型--代码位置速查表)
- [附录 B：API 契约汇总](#附录-bapi-契约汇总)
- [附录 C：术语表](#附录-c术语表)
- [附录 D：参考资料](#附录-d参考资料)

---

## 0. 阅读指南与全局约定

### 0.1 怎么用这份文档

1. **按 Phase 顺序做**：Phase 0 → 1 → 2 → 3 → 4 → 5 → 6。每个 Phase 结束都是一个可交付、可演示的版本。
2. **每个任务的格式**：`任务编号 → 目标 → 改动文件 → 具体做法（含代码骨架）→ 验收标准`。做完一个任务勾一个。
3. **不要跳步**：Phase 1 是 Phase 2 的地基（AI 理解出来的意图需要 Phase 1 的规则底座来承接）。Phase 2 没做完不要开始 Phase 3。
4. **每个 Phase 开工前**，按仓库规则在 `OpenSpec/changes/` 开一个 change 提案（本文档每个 Phase 末尾给了建议的 change id），批准后再动代码。
5. **验证纪律**：每个任务的验收标准必须真实跑过。没跑过的不许勾。测试命令统一是 `npm test`（即 `node --test test/*.js`），单跑排课用 `node --test test/timetable-scheduler.test.js test/timetable-planner-ui.test.js test/timetable-smart-workbench.test.js`。

### 0.2 全局约定（所有 Phase 通用）

| 约定 | 内容 |
|---|---|
| 时间槽写法 | `"天-节"` 字符串，如 `"1-3"` = 周一第 3 节。解析正则见 [view-constraint-dialog.js](public/js/tools/timetable/view-constraint-dialog.js) `requirementSlotLabel`：`/^(\d{1,2})-(\d{1,2})$/` |
| 硬约束 | 排课结果**必须**满足，违反即无效方案（Timefold 里是 `HardSoftScore.ONE_HARD`） |
| 软约束 | **尽量**满足，违反扣分但方案有效（`HardSoftScore.ONE_SOFT`） |
| 前端状态词典 | `actionable/ready/effective`=可应用；`needs_review/review`=需复核；`handled/ignored`=已处理；`candidate/pending`=待确认；`unsupported`=暂不支持；`invalid`=需修正 |
| 后端行状态 | draftRow.status：`effective`（可直接生效）/`suggestion`（建议）/`needs_review`/`ignored`/`invalid`/`unsupported` |
| 沟通语言 | UI 全中文；代码注释与提交信息按仓库规则（提交英文 Conventional Commits） |
| 日志 | 后端结构化日志（不要 `console.log` 到生产路径）；前端调试日志交付前清理 |
| 两条求解路径 | ①本地启发式：`gateway/services/timetable-scheduler.js`；②Timefold：`gateway/services/timetable-solver-bridge.js` + `solver/`（Java）。**每一种新约束必须回答"两条路径各怎么处理"**，允许答案是"本地路径只进评分/审计不参与放置"，但必须显式写出来 |

### 0.3 成功的定义（市场化验收线）

| 指标 | 目标值 | 测法 |
|---|---|---|
| 语料覆盖率 | 内置黄金语料（≥100 句）≥ 95% 能正确结构化（含触发澄清也算正确） | Phase 2 回归测试 |
| 字段准确率 | 黄金语料上抽取的对象/时间/参数字段准确率 ≥ 98% | 同上 |
| 硬约束落地率 | 用户确认后的硬约束 100% 进入 `project.rules` 且两条求解路径都遵守 | Phase 1/4 测试 |
| 澄清率 | 需要反问的输入占比 ≤ 15%（模糊输入除外） | 语料统计 |
| 端到端时长 | 30 班规模：输入 → 复核 → 应用 → 求解 → 报告 ≤ 3 分钟 | 手工冒烟 |
| 可解释性 | 求解后每条用户约束都能回答"满足了没有，没满足为什么" | Phase 4 报告 |

---

## 1. 现状全景（必读）

### 1.1 端到端数据流（现在就是通的，不要推倒重来）

```
用户输入（文本 / Excel / 手动表单）
   │
   ▼
POST /api/tools/timetable/rules/parse          gateway/routes/timetable.js:600
   │  parseTimetableRules()                    gateway/services/timetable-rule-parser.js:4928
   │  ├─ Excel → workbookSheets() 解析          同文件:293
   │  ├─ 本地正则识别（splitSentences 等）
   │  ├─ AI 复审 parseAiOrLocal()/callAiReview() 同文件:4797/3812
   │  └─ 产出 draftRows + requirementItems + semanticActions
   ▼
前端复核台（智能约束助手弹窗）
   │  buildUnifiedRequirementItems()            public/js/tools/timetable/constraint-dialog-review-model.js:197
   │  renderConstraintDialog()                  public/js/tools/timetable/view-constraint-dialog.js:955
   ▼
用户点「应用需求」 applyConstraintsFromDialog()  public/js/tools/timetable/controller-constraint-dialog.js:675
   │
   ├─ 规则类 → POST /rules/normalize → POST /rules
   │            └→ 存入 project.rules            gateway/routes/timetable.js:566
   └─ 计划/优化类 → POST /requirements/apply
                └→ applyTimetableRequirementActions() 改 lessonPlans/软规则
   ▼
排课求解（两条路径）
   ├─ 本地启发式  gateway/services/timetable-scheduler.js（读 project.rules）
   └─ Timefold    gateway/services/timetable-solver-bridge.js
   │                buildTimetableProblem() 同文件:442
   │                ├─ blockedSlotsForPlan() 读 hardRules.teacherUnavailable/classUnavailable 同文件:200-208
   │                ├─ lockedSlots → pinned                                   同文件:238
   │                └─ preferMorning ← softRules.morningSubjects              同文件:378
   ▼
Java 求解器  solver/src/main/java/com/icecream/timetable/
   │  TimetableConstraintProvider.defineConstraints() 24 条约束
   ▼
排课结果 + 满足度评估
   └─ POST /rules/fulfillment                   gateway/routes/timetable.js:712
       gateway/services/timetable-constraint-fulfillment.js
```

### 1.2 文件地图（谁负责什么）

| 文件 | 职责 | 本方案会动它的 Phase |
|---|---|---|
| `public/js/tools/timetable/view-constraint-dialog.js` | 约束助手弹窗渲染（1163 行） | P0, P2, P3, P4 |
| `public/js/tools/timetable/view-constraint-dialog-components.js` | 卡片/编辑表单/AI 聊天面板 | P0, P3, P5 |
| `public/js/tools/timetable/controller-constraint-dialog.js` | 弹窗控制器：解析/应用/澄清（868 行） | P0, P2, P3, P4 |
| `public/js/tools/timetable/constraint-dialog-review-model.js` | 复核台数据模型（345 行，前端"哪些能应用"的唯一裁判） | P0, P1 |
| `public/js/tools/timetable/controller.js` | 排课工具总控制器 | P4, P5 |
| `gateway/routes/timetable.js` | HTTP 路由层 | P1, P3, P5 |
| `gateway/services/timetable-rule-parser.js` | 解析引擎（约 5000 行：本地正则 + AI + 归一化 + 冲突预检 + 语义动作） | P1, P2, P3 |
| `gateway/services/timetable-project.js` | 项目数据模型 `normalizeTimetableProject` / `normalizeRules`（rules 存储 schema 的唯一权威） | P1 |
| `gateway/services/timetable-scheduler.js` | 本地求解 + 审计 + 评分 | P1, P4 |
| `gateway/services/timetable-solver-bridge.js` | Timefold 桥接（problem 构建/结果回填） | P1 |
| `gateway/services/timetable-constraint-fulfillment.js` | 约束满足度评估 | P1, P4 |
| `gateway/services/timetable-conflicts.js` | 冲突检测 | P1 |
| `gateway/services/timetable-agent/skills/solve-plan-skill.js` | 已有 agent 骨架 | P5 |
| `solver/src/main/java/com/icecream/timetable/domain/LessonAssignment.java` | 求解实体（含 subjectPriority/preferMorning/preferLater/allowedRoomIds 等字段） | P1 |
| `solver/src/main/java/com/icecream/timetable/solver/TimetableConstraintProvider.java` | 基础约束（16 条） | P1 |
| `solver/src/main/java/com/icecream/timetable/solver/ChineseEducationConstraints.java` | 中国教育场景约束（8 条） | P1 |
| `test/timetable-scheduler.test.js` `test/timetable-planner-ui.test.js` `test/timetable-smart-workbench.test.js` | 现有测试 | 全部 |

### 1.3 现有约束能力盘点（三张表）

**表 A：解析器认可、能写进 `project.rules` 的 9 种类型**（`SUPPORTED_EFFECTIVE_TYPES`，[timetable-rule-parser.js:28-38](gateway/services/timetable-rule-parser.js#L28-L38)）

| type | 中文名 | 强度 | 存到哪 |
|---|---|---|---|
| `teacher_unavailable` | 教师不可排 | 硬 | `hardRules.teacherUnavailable{teacherId:[slots]}` |
| `class_unavailable` | 班级不可排 | 硬 | `hardRules.classUnavailable{classId:[slots]}` |
| `locked_slot` | 固定课节 | 硬 | `hardRules.lockedSlots[]` |
| `subject_morning` | 上午优先 | 软 | `softRules.morningSubjects[]` |
| `subject_preferred_periods` | 课程优先节次 | 软 | softRules（以 `normalizeRules` 实现为准） |
| `subject_avoid_periods` | 课程避开节次 | 软 | softRules（同上） |
| `teacher_daily_limit` | 教师每日上限 | 软 | `softRules.teacherLimits{teacherId:{daily}}` |
| `teacher_consecutive_limit` | 教师连续上限 | 软 | `softRules.teacherLimits{teacherId:{consecutive}}` |
| `subject_spread` | 课程分散 | 软 | softRules |

**表 B：只作为"建议/已处理"、不进规则存储的类型**（[timetable-rule-parser.js:40](gateway/services/timetable-rule-parser.js#L40) `SUGGESTION_ONLY_TYPES` 及 `requirementStatusForRow`）

| type | 中文名 | 现状 |
|---|---|---|
| `teacher_load_balance` | 教师负载均衡 | suggestion → 走 `/requirements/apply` 的优化目标 |
| `class_daily_balance` | 班级每日均衡 | suggestion |
| `class_subject_spread` | 班级课程分散 | suggestion |
| `block_protection` | 连堂块保护 | handled（求解器天然保证，见表 C `consecutiveBlock`） |

**表 C：Timefold Java 求解器已实现的 24 条约束**（[TimetableConstraintProvider.java:23-53](solver/src/main/java/com/icecream/timetable/solver/TimetableConstraintProvider.java#L23-L53)）

| # | Java 约束 | 硬/软 | 说明 | 前端能控制吗？ |
|---|---|---|---|---|
| 1 | classConflict | 硬 | 班级时间冲突 | 自动，无需控制 |
| 2 | teacherConflict | 硬 | 教师时间冲突 | 自动 |
| 3 | pinnedTime | 硬 | 固定课节 | ✅ locked_slot |
| 4 | blockedTime | 硬 | 不可排时间 | ✅ teacher/class_unavailable |
| 5 | roomRequirement | 硬 | 教室要求 | ❌ **没接**（数据来自 lessonPlan.roomId，规则层没有入口） |
| 6 | roomConflict | 硬 | 教室冲突 | ❌ 同上 |
| 7 | consecutiveBlock | 硬 | 连堂块完整 | 自动 |
| 8 | spreadSameCourse | 软 | 同课分散 | 部分（subject_spread） |
| 9 | avoidAdjacentSameCourse | 软 | 避免相邻同课 | ❌ 没接 |
| 10 | mainSubjectsEarlier | 软 | 主课靠前 | 半接：`preferMorning` ← morningSubjects；`subjectPriority>=90` 兜底 |
| 11 | practicalSubjectsLater | 软 | 术科靠后 | ❌ **靠正则猜科目名**（[solver-bridge:379](gateway/services/timetable-solver-bridge.js#L379) `/pe|music|art|lab|sport|physical/i`），规则层无入口 |
| 12 | teacherDailyLoad | 软 | 教师每日负载 | 部分 |
| 13 | classDailyLoad | 软 | 班级每日负载 | ❌ 没接 |
| 14 | teacherLunchBridge | 软 | 教师午间跨段连课 | ❌ 没接 |
| 15 | teacherGap | 软 | 教师空堂 | ❌ 没接 |
| 16 | sameCourseHalfDaySplit | 软 | 同课上下午分裂 | ❌ 没接 |
| 17 | mainSubjectGoldenHourPreference | 软 | 主课黄金时段 | ❌ 没接 |
| 18 | sportsClassDistribution | 软 | 体育课分布 | ❌ 没接 |
| 19 | teacherContinuousTeachingLimit | 软 | 教师连续教学限制 | 部分 |
| 20 | afternoonFatigueAvoidance | 软 | 下午疲劳规避 | ❌ 没接 |
| 21 | laboratoryRoomRequirement | 软 | 实验室要求 | ❌ 没接 |
| 22 | sameSubjectPreparationTimeGap | 软 | 同科备课间隔 | ❌ 没接 |
| 23 | teacherDailyLoadVarianceMinimization | 软 | 教师负载方差最小化 | ❌ 没接 |
| 24 | walkingClassTimeAlignment | 软 | 走班时间对齐 | ❌ 没接 |

> **核心结论**：求解器的能力**远大于**前端暴露的能力。市场化第一步不是造新求解能力，而是把已有能力接上规则层（Phase 1 批 A 就是干这个，投入小、见效快）。

### 1.4 已知问题清单（本轮审查发现）

| 编号 | 问题 | 位置 | 修复 Phase |
|---|---|---|---|
| P-1 | "已理解需求 (86)" 混淆：系统自动补充的规则和用户真实输入混在一起计数，用户输入 17 条却显示 86 条 | `buildRequirementSemantics` 把 `systemRequirementsFromText` 等自动项与用户行合并（[timetable-rule-parser.js:2376-2387](gateway/services/timetable-rule-parser.js#L2376)） | Phase 0 |
| P-2 | 大量"需求"无法生成机器规则，用户看不出哪些真的会生效 | 前端 `BACKEND_RULE_TYPES` 只有 9 种（[constraint-dialog-review-model.js:9-19](public/js/tools/timetable/constraint-dialog-review-model.js#L9)） | Phase 0（讲清楚）+ Phase 1（真扩容） |
| P-3 | `preferLater`（术科靠后）靠科目名英文正则猜，中文科目全靠 subjectId 碰运气 | [solver-bridge:379](gateway/services/timetable-solver-bridge.js#L379) | Phase 1 批 A |
| P-4 | 教室约束求解器支持但规则层没入口；weekPattern 只在 complex 模型下半支持 | 表 C #5/#6 | Phase 1 批 B / Phase 6 |
| P-5 | 状态词典有三套（draftRow.status / requirement.status / action.status），映射零散易错 | review-model + view 两处 | Phase 0 |
| P-6 | "AI 已复审 AI 已复审此项识别结果。"重复文案 | view-constraint-dialog.js `renderRequirementAiReview` | ✅ 已修复（本轮） |
| P-7 | AI 复审只做"复审"，不做主抽取；本地正则模式有限，模糊表达直接漏 | `parseAiOrLocal` 本地优先策略 | Phase 2 |
| P-8 | 澄清只支持单字段一问一答，选项类澄清 UI 不完整 | `renderRequirementClarification` | Phase 3 |
| P-9 | 求解后用户看不到"我的约束满足了没有" | fulfillment 有接口无完整 UI 闭环 | Phase 4 |

---

## 2. 三层架构与约束类型总目录

### 2.1 三层模型（本方案最重要的设计决策）

要做到"几乎所有自然语言都能理解"，**不能**给每种说法都造一个新存储字段和新求解约束——那样类型会爆炸。正确做法是三层：

```
┌─────────────────────────────────────────────────────────┐
│ 意图层 Intent（宽，~50+ 种，允许无限扩展）                  │
│   自然语言 → AI/正则识别出的"用户想要什么"                  │
│   例：avoid_last_period（避开最后一节）                    │
├─────────────────────────────────────────────────────────┤
│ 规则层 Primitive（窄，~20 种，存储原语，谨慎扩展）           │
│   意图经「编译」落到 project.rules 的规范字段                │
│   例：avoid_last_period ──编译──▶ subject_avoid_periods    │
│        （slots = 每天最后一节的槽位列表）                    │
├─────────────────────────────────────────────────────────┤
│ 求解层 Constraint（双路径：本地启发式 + Timefold Java）      │
│   规则原语被两条求解路径消费                                │
└─────────────────────────────────────────────────────────┘
```

**规矩**：
1. 新增一种"用户说法"（意图），优先问：能否编译到已有原语？能就只改编译器（`compileRequirementToRows`，Phase 2 建），不动存储和求解器。
2. 只有编译不出来时才新增原语（走 Phase 1 的"黄金清单"十步流程）。
3. 意图层的类型名可以随便加；原语层的类型名一旦发布不许改（向后兼容）。

### 2.2 约束类型总目录（目标 30 种意图，落到 22 种原语）

**图例**：✅=已有 ｜ 🔌=求解器已支持只需接线（批 A）｜ 🆕=需新增求解逻辑（批 B）｜ 🍬=语法糖，编译到其它原语（批 C）｜ 🧩=复杂模型（Phase 6）

#### 组 1：时间可用性（硬约束为主）

| # | 意图 type | 中文名 | 例句 | 参数 | 编译到原语 | 状态 |
|---|---|---|---|---|---|---|
| 1 | `teacher_unavailable` | 教师不可排 | "张老师周一上午不排课" | teacherId, slots[] | 自身 | ✅ |
| 2 | `class_unavailable` | 班级不可排 | "三(2)班周五下午社团活动" | classId, slots[] | 自身 | ✅ |
| 3 | `global_unavailable` | 全校不可排 | "周一第1节全校升旗不排课" | slots[] | 自身（新原语） | 🆕 |
| 4 | `lunch_protection` | 午休保护 | "中午最后一节和下午第一节之间不要连排" | — | `global_unavailable` 或教师午桥权重 | 🍬 |
| 5 | `teaching_group_meeting` | 教研活动 | "数学组周三下午集体教研" | subjectId/groupId, slots[] | 批量 `teacher_unavailable`（该组全部教师） | 🍬 |
| 6 | `locked_slot` | 固定课节 | "班会固定周一第1节" | classId, subjectId, teacherId, day, period | 自身 | ✅ |
| 7 | `first_period_assign` | 早读/首节固定 | "语文早读排第1节" | 同 locked_slot | `locked_slot` | 🍬 |

#### 组 2：时段偏好（软约束为主）

| # | 意图 type | 中文名 | 例句 | 参数 | 编译到原语 | 状态 |
|---|---|---|---|---|---|---|
| 8 | `subject_morning` | 上午优先 | "数学尽量排上午" | subjectId | 自身 | ✅ |
| 9 | `subject_afternoon` | 下午优先 | "体育尽量排下午" | subjectId | 自身（新原语 `afternoonSubjects`） | 🔌（Java `practicalSubjectsLater`/`preferLater` 已有） |
| 10 | `subject_preferred_periods` | 优先节次 | "英语优先第2-4节" | subjectId, slots[] | 自身 | ✅ |
| 11 | `subject_avoid_periods` | 避开节次 | "体育避开第一节" | subjectId, slots[] | 自身 | ✅ |
| 12 | `avoid_first_period` | 避开第一节 | "音乐不要排第一节" | subjectId | `subject_avoid_periods`(slots=每天第1节) | 🍬 |
| 13 | `avoid_last_period` | 避开最后一节 | "实验课别放最后一节" | subjectId | `subject_avoid_periods`(slots=每天最后1节) | 🍬 |
| 14 | `golden_hour_preference` | 主课黄金时段 | "主科放精力好的时段" | subjectIds[] | `subject_morning` + priority 权重 | 🍬（Java #17 已有） |

#### 组 3：课时分布

| # | 意图 type | 中文名 | 例句 | 参数 | 编译到原语 | 状态 |
|---|---|---|---|---|---|---|
| 15 | `subject_spread` | 课程分散 | "数学每周分散排，别挤同一天" | subjectId | 自身 | ✅ |
| 16 | `subject_daily_limit` | 科目每日上限 | "同一天数学不超过2节" | subjectId, limit | 自身（新原语） | 🆕 |
| 17 | `course_interval` | 同科间隔 | "两次物理课至少隔一天" | subjectId, minGapDays | `subject_spread` 加参数 | 🍬→🆕（参数化 spread） |
| 18 | `class_daily_subject_balance` | 班级每日均衡 | "每个班每天主课别超过4节" | classScope, limit | 自身（正式化现 suggestion） | 🔌（Java #13/#23 已有） |
| 19 | `week_pattern` | 单双周 | "信息课单周上" | subjectId/planId, odd/even | complex 模型字段 | 🧩 |

#### 组 4：教师负载

| # | 意图 type | 中文名 | 例句 | 参数 | 编译到原语 | 状态 |
|---|---|---|---|---|---|---|
| 20 | `teacher_daily_limit` | 教师每日上限 | "每位老师每天最多5节" | teacherId/all, limit | 自身 | ✅ |
| 21 | `teacher_consecutive_limit` | 教师连续上限 | "老师连堂别超过3节" | teacherId/all, limit | 自身 | ✅ |
| 22 | `teacher_weekly_limit` | 教师每周上限 | "李老师每周不超过16节" | teacherId, limit | 自身（新原语） | 🆕 |
| 23 | `teacher_max_days_per_week` | 每周最多上课天数 | "王老师每周最多来4天" | teacherId, limit | 自身（新原语） | 🆕 |
| 24 | `teacher_gap_preference` | 少空堂 | "老师的课尽量连着，少空堂" | teacherId/all, weight | 自身（新原语，开关+权重） | 🔌（Java #15 已有） |
| 25 | `teacher_load_balance` | 负载均衡 | "各老师课时尽量均衡" | weight | 自身（suggestion 正式化） | 🔌（Java #23 已有） |

#### 组 5：关系约束

| # | 意图 type | 中文名 | 例句 | 参数 | 编译到原语 | 状态 |
|---|---|---|---|---|---|---|
| 26 | `teacher_mutual_exclusion` | 教师互斥 | "张老师和李老师不要同时有课（拼车）" | teacherIds[2+] | 自身（新原语） | 🆕 |
| 27 | `subject_not_same_day` | 科目不同天 | "物理实验和化学实验别放同一天" | subjectIds[2], classScope | 自身（新原语） | 🆕 |
| 28 | `subject_sequence` | 科目先后 | "体育课后别马上考试科目" | beforeId, afterId | 自身（新原语，软） | 🆕 |

#### 组 6：空间与复杂模型

| # | 意图 type | 中文名 | 例句 | 参数 | 编译到原语 | 状态 |
|---|---|---|---|---|---|---|
| 29 | `room_requirement` | 教室要求 | "物理实验必须在实验室上" | subjectId, roomIds[]/roomType | 自身（新原语） | 🔌（Java #5/#6/#21 已有，bridge 有 `allowedRoomIds`） |
| 30 | `campus_commute` | 跨校区间隔 | "两校区之间要留2节过渡" | teacherId, gapPeriods | complex（`commuteRules` 已有骨架） | 🧩 |

> 连堂类（"物理每周一次双连堂"）已由 `lesson_plan_patch` 语义动作走 `/requirements/apply` 修改任课计划 `blockSize`，不属规则原语，维持现状，Phase 0 只把展示讲清楚。

---

## 3. Phase 0 — 信任修复：把解析结果讲清楚

> **目标**：不改任何求解行为，只改"说法"，让用户第一眼就明白：哪些是我说的、哪些是系统送的、哪些会真的生效、生效到哪里。
> **为什么先做**：这是用户对产品信任的地基。86 条的混乱不修，后面做得再智能用户也不敢用。
> **建议 change id**：`constraint-review-clarity`

### 任务 0.1 需求来源标记（后端小改）

**改动文件**：`gateway/services/timetable-rule-parser.js`

**做法**：给每个 requirementItem 加 `origin` 字段，三个值：`user_input` / `system_supplement` / `manual`。

1. 找到 `textRequirementBase(...)`（系统自动生成需求的公共构造函数，被 `systemRequirementsFromText` / `blockPreferenceRequirementsFromText` / `optimizationRequirementsFromText` / `complexRequirementsFromText` 调用）。凡 id 以 `req_system_` / `req_optimization_` / `req_complex_` 开头的，`origin: 'system_supplement'`；`req_block_` 这类由用户句子触发但对象是系统推导的，`origin: 'user_input'`（它有原句 evidence）。
2. `requirementFromRow()`（[timetable-rule-parser.js:1585](gateway/services/timetable-rule-parser.js#L1585)）加 `origin: 'user_input'`。
3. 前端 `manualRequirementFromConstraint()`（[controller-constraint-dialog.js:155](public/js/tools/timetable/controller-constraint-dialog.js#L155)）加 `origin: 'manual'`。
4. 判定优先级：有 `sourceRow`（来自文件具体行）→ 一定是 `user_input`。

**验收**：`/rules/parse` 返回的每个 requirementItem 都带 `origin`；对 `AI排课约束建议.xlsx` 解析，来自表格行的都是 `user_input`。

### 任务 0.2 双计数标题 + 来源徽章（前端）

**改动文件**：`public/js/tools/timetable/view-constraint-dialog.js`

**做法**：
1. 改 [view-constraint-dialog.js:923-924](public/js/tools/timetable/view-constraint-dialog.js#L923) 的标题区：

```javascript
// 原：
// <strong>已理解需求 (${requirements.length})</strong>
// <span>${visibleRequirements.length} 条正在显示 · 当前筛选可应用 ${summary.applicable} 项</span>

// 改为：
const userItems = requirements.filter(item => (item.origin || 'user_input') !== 'system_supplement');
const systemItems = requirements.filter(item => item.origin === 'system_supplement');
// 标题：
// <strong>解析结果</strong>
// <span>来自你的输入 ${userItems.length} 条 · 系统补充 ${systemItems.length} 条 · 本次可写入排课 ${summary.applicable} 条</span>
```

2. `requirementSourceLabel()`（[view-constraint-dialog.js:284-296](public/js/tools/timetable/view-constraint-dialog.js#L284)）的兜底文案 `'识别结果'` 改为按 origin 显示：`system_supplement → '系统补充'`，`manual → '手动添加'`，其余保留"输入文本 / xx表 第 N 行"。
3. 表格每行状态列旁加来源小徽章（复用现有 `<small>` 样式）：`我的输入` / `系统` / `手动`。

**验收**：截图场景下标题显示"来自你的输入 17 条 · 系统补充 69 条"（数字以实际为准）；每行能看出来源。

### 任务 0.3 系统补充分组默认折叠

**改动文件**：`view-constraint-dialog.js`（渲染）、`controller-constraint-dialog.js`（折叠状态）

**做法**：
1. `state.constraintDialog.systemGroupCollapsed` 默认 `true`。
2. 列表渲染时，`origin === 'system_supplement'` 的行折叠进一个可展开区块：

```
▸ 系统补充的默认规则 (69 条) —— 时间冲突检查、连堂保护等，系统会自动遵守  [展开]
```

3. 点击切换 `data-action="toggle-system-group"`，在 `grid-interactions.js` 事件表里登记（参照现有 `filter-requirements` 的写法）。
4. 筛选器切到"已自动处理"时自动展开。

**验收**：打开弹窗默认只看到用户输入的行；点展开能看到全部系统项；计数不变。

### 任务 0.4 "落地去向"列改造（把 P-2 讲清楚）

**改动文件**：`view-constraint-dialog.js` `renderRequirementRow` / `renderRequirementDetail`

**做法**：现有"落点"列的文案换成带图标的去向徽章，词典统一为：

| applyTo/status | 徽章文案 | 颜色 tone |
|---|---|---|
| rule + actionable | `→ 排课规则` | 绿 |
| lesson_plan | `→ 任课计划` | 绿 |
| optimization | `→ 优化目标` | 蓝 |
| handled / solver_policy | `✓ 系统内置` | 灰 |
| review / needs_review | `⚠ 待你确认` | 黄 |
| unsupported | `✗ 暂不支持` | 红 |

详情面板"落点"行下补一句人话解释（新增 `applyToExplanation()` 函数）：
- 排课规则："应用后写入项目规则，下次排课必须/尽量遵守。"
- 系统内置："排课引擎本来就会保证这一点，无需应用。"
- 待你确认："信息不全或没匹配到对象，补充后才能应用。"
- 暂不支持："当前版本无法自动实现，可作为人工调课参考。"

**验收**：任选 5 条不同状态的行，详情面板都能看到一句人话解释。

### 任务 0.5 应用按钮语义强化

**改动文件**：`view-constraint-dialog.js`（按钮区，[view-constraint-dialog.js:997-1008](public/js/tools/timetable/view-constraint-dialog.js#L997)）、`controller-constraint-dialog.js`（确认弹窗文案，[controller-constraint-dialog.js:695-697](public/js/tools/timetable/controller-constraint-dialog.js#L695)）

**做法**：
1. 按钮下加小字：`将写入排课规则，立即参与下次排课`。
2. 确认弹窗文案改为分类明细：

```
确定应用这 17 条需求吗？
├ 12 条 → 排课硬规则（必须遵守）
├ 3 条 → 排课软规则（尽量满足）
└ 2 条 → 任课计划调整（连堂设置）
应用后立刻生效，下次排课就会使用。
```

统计逻辑复用 `getBackendRuleRows` / `getApplicableSemanticActions`（[constraint-dialog-review-model.js:324-340](public/js/tools/timetable/constraint-dialog-review-model.js#L324)），按 `row.priority === 'hard'` 分硬软。

3. 应用成功后的 `alert` 改为具体反馈："已写入 12 条硬规则、3 条软规则，更新 2 个任课计划。共 17 条已生效。"

**验收**：手工走一遍应用流程，三处文案都符合上述格式。

### 任务 0.6 状态词典唯一化

**改动文件**：新建 `public/js/tools/timetable/constraint-status-dict.js`

**做法**：把 `requirementStatusLabel` / `requirementApplyLabel` / `semanticActionStatusLabel` / `RULE_TYPE_LABELS` / `requirementIntentLabel` 的映射表全部搬进这个新文件统一导出，view 文件只 import 不再内联定义（消除 P-5 三处不一致的土壤）。**只搬家不改值**（0.4 已改的值除外）。

**验收**：`npm test` 全绿；grep 确认 view-constraint-dialog.js 里不再有内联的 status 映射字面量。

### Phase 0 测试补充

在 `test/timetable-planner-ui.test.js` 新增断言：
- render 后 HTML 含 `来自你的输入`、`系统补充`；
- `origin==='system_supplement'` 的行默认不可见（折叠容器内）；
- 确认弹窗文案含"硬规则"字样（mock confirm 捕获文案）。

### Phase 0 验收清单

- [ ] 0.1 origin 字段后端落地
- [ ] 0.2 双计数标题 + 来源徽章
- [ ] 0.3 系统组折叠
- [ ] 0.4 去向徽章 + 人话解释
- [ ] 0.5 应用按钮/确认/成功反馈三段文案
- [ ] 0.6 状态词典唯一化
- [ ] `npm test` 全绿 + 浏览器手工冒烟（上传 `AI排课约束建议.xlsx` 走完整流程）

---

## 4. Phase 1 — 规则底座扩容（9 种 → 22+ 种）

> **目标**：把"能存、能解、能被两条求解路径遵守"的原语从 9 种扩到 22 种，优先接通求解器已有能力。
> **建议 change id**：`rules-schema-v2`（批 A+存储）、`rules-new-constraints`（批 B）、`intent-sugar-compiler`（批 C）

### 4.1 目标存储 schema：`project.rules` v2

**改动文件**：`gateway/services/timetable-project.js` 的 `normalizeRules()`（[timetable-project.js:813](gateway/services/timetable-project.js#L813)）

**原则**：只加字段不改老字段；所有新字段缺省为空；旧项目数据读入后不报错（向后兼容由 `|| {}`/`|| []` 兜底保证）。

```javascript
function normalizeRules(raw = {}, enabled = false) {
    const hardRules = raw.hardRules || {};
    const softRules = raw.softRules || {};
    return {
        hardRules: {
            // ===== 现有（不动）=====
            lockedSlots: normalizeLockedSlots(hardRules.lockedSlots, enabled),
            teacherUnavailable: normalizeRuleMap(hardRules.teacherUnavailable),
            classUnavailable: normalizeRuleMap(hardRules.classUnavailable),
            // ===== v2 新增 =====
            globalUnavailable: normalizeSlotList(hardRules.globalUnavailable),        // ["1-1","2-1"]
            subjectDailyLimit: normalizeIntMap(hardRules.subjectDailyLimit, 1, 8),    // {subjectId: 2}
            teacherWeeklyLimit: normalizeIntMap(hardRules.teacherWeeklyLimit, 1, 40), // {teacherId: 16}
            teacherMaxDaysPerWeek: normalizeIntMap(hardRules.teacherMaxDaysPerWeek, 1, 7),
            teacherMutualExclusion: normalizeIdGroupList(hardRules.teacherMutualExclusion),
            //   [{ teacherIds: ["t1","t2"] }]
            subjectNotSameDay: normalizeSubjectPairList(hardRules.subjectNotSameDay),
            //   [{ subjectIds: ["s1","s2"], classIds: [] /*空=全部班*/ }]
            roomRequirements: normalizeRoomRequirements(hardRules.roomRequirements),
            //   { subjectId: { roomIds: ["r1","r2"] } }
        },
        softRules: {
            // ===== 现有（保持 normalizeRules 现实现，不删任何字段）=====
            morningSubjects: /* 现有实现 */,
            // ...其余现有 softRules 字段原样保留...
            // ===== v2 新增 =====
            afternoonSubjects: normalizeIdList(softRules.afternoonSubjects),
            subjectDailySoftLimit: normalizeIntMap(softRules.subjectDailySoftLimit, 1, 8),
            subjectSequence: normalizeSequenceList(softRules.subjectSequence),
            //   [{ beforeSubjectId, afterSubjectId, weight: 1 }]
            teacherGapWeight: intInRange(softRules.teacherGapWeight, 0, 0, 10),   // 0=按引擎默认
            classDailyBalance: {
                enabled: Boolean(softRules.classDailyBalance?.enabled),
                mainSubjectDailyMax: intInRange(softRules.classDailyBalance?.mainSubjectDailyMax, 0, 0, 8),
            },
            teacherLoadBalance: {
                enabled: Boolean(softRules.teacherLoadBalance?.enabled),
                weight: intInRange(softRules.teacherLoadBalance?.weight, 1, 1, 10),
            },
        },
    };
}
```

新增 4 个小工具函数（同文件）：`normalizeSlotList`（复用现有 slot 校验逻辑）、`normalizeIntMap(raw,min,max)`、`normalizeIdGroupList`、`normalizeSubjectPairList`、`normalizeRoomRequirements`——全部是"过滤非法值、裁剪长度、String 化 id"的纯函数，参照 `normalizeRuleMap` 的写法。

**验收**：新旧两种 rules JSON 各构造一份过 `normalizeTimetableProject` 不抛错；新字段乱传（负数/超长/非法 id）都被清洗；`test/timetable-scheduler.test.js` 加 schema 归一化单测。

### 4.2 新原语实施"黄金清单"（每种类型都走这 10 步）

> 这是本 Phase 的核心方法论。**每接一种类型，把这张表复制一份填完。** 缺一步就不算完成。

| 步骤 | 位置 | 要做的事 |
|---|---|---|
| ① 词典 | `constraint-status-dict.js`（前端，Phase 0 建）+ `RULE_TYPE_LABELS` | 加中文标签 |
| ② 后端类型准入 | `timetable-rule-parser.js` `SUPPORTED_EFFECTIVE_TYPES`（硬/直接生效）或 `SUGGESTION_ONLY_TYPES` | 加 type |
| ③ 前端类型准入 | `constraint-dialog-review-model.js` `BACKEND_RULE_TYPES` | 加 type（决定"可应用"判定） |
| ④ 解析产出 | `timetable-rule-parser.js` 本地正则分支 + Phase 2 的 AI 目录 | 能从例句产出 draftRow |
| ⑤ 归一化写入 | `timetable-rule-parser.js` 里把 draftRows 编成 `draftRules`（project.rules 形状）的函数（搜 `draftRules` 赋值处） | 新 type 写入 4.1 对应字段 |
| ⑥ 存储 | `timetable-project.js` `normalizeRules` | 4.1 已做 |
| ⑦ 本地求解 | `timetable-scheduler.js` | 硬约束→加入放置候选过滤（找现有 `teacherUnavailable` 判定处并列添加）；软约束→加入 `buildTimetableScore`/audit 扣分项 |
| ⑧ Timefold | `timetable-solver-bridge.js` `buildTimetableProblem` 传字段 + Java 端 domain 字段 + Constraint | 见 4.4 |
| ⑨ 冲突/满足度 | `detectRuleConflicts`（[timetable-rule-parser.js:2633](gateway/services/timetable-rule-parser.js#L2633)）+ `timetable-constraint-fulfillment.js` | 预检矛盾 + 求解后可评估 |
| ⑩ 测试 | 三个 test 文件 | 解析单测 + 归一化单测 + 求解冒烟 + UI render 断言 |

### 4.3 批 A —— 纯接线（求解器已支持，先做，见效最快）

#### A-1 `subject_afternoon`（下午优先）——顺手修 P-3

1. schema：`softRules.afternoonSubjects[]`（4.1 已加）。
2. bridge 改 [solver-bridge:379](gateway/services/timetable-solver-bridge.js#L379)：

```javascript
// 原：preferLater: /pe|music|art|lab|sport|physical/i.test(`${plan.subjectId} ${subject?.name || ''}`),
// 改：规则优先，正则只做兜底建议
preferLater: Boolean(
    project.rules?.softRules?.afternoonSubjects?.includes(plan.subjectId)
    || (!project.rules?.softRules?.afternoonSubjects?.length
        && /pe|music|art|lab|sport|physical|体育|音乐|美术|实验/i.test(`${plan.subjectId} ${subject?.name || ''}`))
),
```

3. 本地路径：`timetable-scheduler.js` 评分处给下午槽位的 afternoonSubjects 加正分（找 morningSubjects 的评分点并列写）。
4. 解析：本地正则 `/(下午|午后).*(优先|尽量|排)|尽量.*下午/` + 科目提取（参照 `subject_morning` 分支复制改造）。

#### A-2 `room_requirement`（教室要求）

1. schema：`hardRules.roomRequirements{subjectId:{roomIds[]}}`。
2. bridge：`buildTimetableProblem` 里 `allowedRoomIds` 的来源扩为 `lessonPlan.roomId ∪ rules.hardRules.roomRequirements[plan.subjectId].roomIds`（找 `allowedRoomIds` 赋值处，[solver-bridge:372-373](gateway/services/timetable-solver-bridge.js#L372) 附近的构造函数）。
3. Java：`roomRequirement`/`roomConflict` 已有（表 C #5/#6），**无需改**。
4. 本地路径：本地求解若暂不支持教室维度，则明确降级——该规则只在 Timefold 路径生效，`fulfillment` 评估时对本地结果标注"本地引擎未评估教室约束"。**把这句话写进 UI 提示**（诚实降级）。
5. 前置条件：项目需有教室数据（`collectRooms(project)` 已存在）。若项目无教室数据，解析时给 `needs_review` + 澄清"项目还没录入教室，先去基础数据添加"。

#### A-3 `class_daily_subject_balance` 正式化（原 suggestion）

1. schema：`softRules.classDailyBalance{enabled, mainSubjectDailyMax}`。
2. `requirementStatusForRow`（[timetable-rule-parser.js:1566](gateway/services/timetable-rule-parser.js#L1566)）里 `class_daily_balance` 从 suggestion-only 升为可 `effective`。
3. Java `classDailyLoad`（#13）已有基础版；`mainSubjectDailyMax` 需要在 bridge 给 assignment 传 `classMainDailyMax` + Java 加 groupBy 约束（做法同 4.4 的 B-2 模板）。
4. 本地路径：audit 已有班级负载概念（`buildTimetableQualityIssues`），把超限计入扣分。

#### A-4 `teacher_gap_preference` / `teacher_load_balance` 开关化

1. schema：`softRules.teacherGapWeight` / `softRules.teacherLoadBalance`。
2. bridge：problem JSON 加顶层 `solverWeights: { teacherGap, teacherLoadBalance }`；Java `TimetableSolution` 加对应字段，Constraint 里用 `penalize(..., (a,b) -> basePenalty * weight)`。weight=0 表示用默认。
3. 解析："少空堂/别有空堂/课连着上" → teacher_gap_preference；"均衡/公平/差不多" → teacher_load_balance。

**批 A 验收**：四种类型各写 2 个解析单测句；构造小项目跑 Timefold 冒烟（体育进下午、实验课进实验室）；`npm test` 全绿。

### 4.4 批 B —— 新增求解逻辑（双路径都要写）

以下每种都按 4.2 十步走。这里给出关键代码骨架，其余步骤照抄批 A 的模式。

#### B-1 `global_unavailable`（全校不可排）

- **本地路径**：放置候选过滤——凡 slot ∈ globalUnavailable 直接跳过（加在 `blockedSlotsForPlan` 同层，[solver-bridge:200](gateway/services/timetable-solver-bridge.js#L200) 是 bridge 的；本地 scheduler 里找等价过滤点）。
- **Timefold**：最省事的实现是 bridge 端把 globalUnavailable 并进**每个** assignment 的 `blockedTimeSlotIds`：

```javascript
function blockedSlotsForPlan(project, plan) {
    const blocked = new Set(project.rules?.hardRules?.classUnavailable?.[plan.classId] || []);
    for (const key of project.rules?.hardRules?.globalUnavailable || []) blocked.add(key);  // ← 新增一行
    // ...现有教师部分不动...
}
```

Java 端 `blockedTime` 约束自动生效，**不用改 Java**。

#### B-2 `subject_daily_limit`（科目每日上限）—— Java groupBy 模板（其余 B 类照此模式）

- bridge：assignment 加 `subjectDailyMax`（денormalize，参照 `subjectPriority` 的传法）：

```javascript
subjectDailyMax: project.rules?.hardRules?.subjectDailyLimit?.[plan.subjectId] || 0,
```

- Java `LessonAssignment` 加字段 `private int subjectDailyMax;` + getter/setter + 辅助 `public String classSubjectDayKey()`（`classId|subjectId|weekday`，timeSlot 为 null 时返回 null）。
- Java 约束（`TimetableConstraintProvider`）：

```java
Constraint subjectDailyLimit(ConstraintFactory factory) {
    return factory.forEach(LessonAssignment.class)
            .filter(l -> l.getTimeSlot() != null && l.getSubjectDailyMax() > 0)
            .groupBy(LessonAssignment::classSubjectDayKey,
                     l -> l.getSubjectDailyMax(),
                     ai.timefold.solver.core.api.score.stream.ConstraintCollectors.count())
            .filter((key, max, count) -> count > max)
            .penalize(HardSoftScore.ONE_HARD, (key, max, count) -> count - max)
            .asConstraint("Subject daily limit");
}
```

- 本地路径：放置时统计当日该班该科已放节数，≥limit 的槽位跳过。
- 注意连堂：blockSize=2 的连堂算 2 节，limit=2 时允许一个双连堂但不允许再加第三节。

#### B-3 `teacher_weekly_limit` / `teacher_max_days_per_week`

同 B-2 模板：assignment денormalize `teacherWeeklyMax` / `teacherMaxDays`，groupBy teacherId（weekly）或 teacherId 计 distinct weekday（用 `ConstraintCollectors.countDistinct(l -> l.getTimeSlot().getWeekday())`）。本地路径在放置前做同样计数过滤。
**冲突预检必做**（⑨）：若 `teacherWeeklyLimit < 该教师任课计划总课时`，在 `detectRuleConflicts` 产出 blocking 冲突："李老师每周上限 16 节，但任课计划共 18 节，无解。"——这一步是市场口碑关键，宁可解析后立刻告知，不要让用户排完才发现失败。

#### B-4 `teacher_mutual_exclusion`

- bridge：assignment 加 `mutualExclusionGroups: ["g1"]`（该教师所属互斥组 id 列表）。
- Java：pairwise（参照 `teacherConflict` 写法）：

```java
Constraint teacherMutualExclusion(ConstraintFactory factory) {
    return factory.forEachUniquePair(LessonAssignment.class,
                    Joiners.equal(LessonAssignment::getTimeSlot))
            .filter((l, r) -> l.getTimeSlot() != null && l.sharesMutualExclusionGroup(r))
            .penalize(HardSoftScore.ONE_HARD)
            .asConstraint("Teacher mutual exclusion");
}
```

- 本地路径：放置时检查同 slot 是否已有互斥组教师。

#### B-5 `subject_not_same_day` / B-6 `subject_sequence`

- not_same_day：assignment 加 `notSameDaySubjectIds[]`（同班配对科目），pairwise 同班同天且互为配对 → 按 strength 硬/软扣分。
- sequence：软约束，pairwise 同班同天，`before` 科目节次 > `after` 科目节次 → 扣分。
- 本地路径：sequence 只进评分不做放置过滤（软）；not_same_day 硬时进过滤。

#### B-7 `course_interval`（subject_spread 参数化）

- schema：`softRules` 里 spread 相关结构加 `minGapDays`（默认 1 保持现行为）。
- Java `spreadSameCourse`（#8）现在惩罚同天；扩展为惩罚 `|weekdayA - weekdayB| < minGapDays`（bridge денormalize `spreadMinGapDays`）。

**批 B 验收**：每种类型 1 个"从句子到求解结果"的端到端测试（构造 6 班小项目，断言结果满足约束）；无解场景的冲突预检测试（B-3）；`solver/` 侧 `mvnw test` 通过。

### 4.5 批 C —— 语法糖编译器

**新建文件**：`gateway/services/timetable-intent-compiler.js`

```javascript
/**
 * 意图 → 规则原语 编译器（Phase 1 批 C 建立，Phase 2 的 AI 输出也走这里）
 * 输入：requirement（意图层对象）+ project
 * 输出：draftRow[]（原语层，type ∈ SUPPORTED_EFFECTIVE_TYPES ∪ 批A/B 新原语）
 */
const INTENT_COMPILERS = {
    avoid_first_period: (req, project) => [{
        type: 'subject_avoid_periods',
        targetType: 'subject', targetId: req.targetId, targetName: req.targetName,
        slots: activeWeekdaysOf(project).map(day => `${day}-1`),
        priority: 'soft', status: 'effective',
        sourceText: req.sourceText, confidence: req.confidence,
    }],
    avoid_last_period: (req, project) => [/* 同上，period = 每天最后一个活跃节次 */],
    lunch_protection: (req, project) => [{
        type: 'global_unavailable',
        slots: lunchBoundarySlots(project),   // 由 dayPartBoundaries 推出
        priority: 'hard', status: 'effective', ...
    }],
    teaching_group_meeting: (req, project) => {
        const teacherIds = teachersOfSubject(project, req.subjectId);
        return teacherIds.map(id => ({
            type: 'teacher_unavailable', targetType: 'teacher', targetId: id,
            slots: req.slots, priority: 'hard', status: 'effective',
            sourceText: req.sourceText,
            groupTag: `teaching_group:${req.subjectId}`,   // 溯源：一键整组撤销用
        }));
    },
    first_period_assign: (req, project) => [/* → locked_slot */],
    golden_hour_preference: (req, project) => [/* → subject_morning × N 主科 */],
};

export function compileRequirementToRows(requirement, project) {
    const compiler = INTENT_COMPILERS[requirement.intent];
    if (compiler) return compiler(requirement, project);
    return null; // 不是语法糖，走原语直通
}
```

在 `buildRequirementSemantics` 产出 requirementItems 后调用编译器补 draftRows，并给编译产物 requirementItem 记 `machineRules`（复用现有 `rowId/requirementId` 关联机制，[constraint-dialog-review-model.js:223-239](public/js/tools/timetable/constraint-dialog-review-model.js#L223)）。

**验收**："音乐不要排第一节"产出 5 条 `1-1..5-1` 的 avoid slots；"数学组周三下午教研"给数学组每位教师产出 teacher_unavailable，且 UI 上显示为**一条**需求（多条 machineRules 折在落地结果里——现有 UI 已支持一对多，[view-constraint-dialog.js:723-760](public/js/tools/timetable/view-constraint-dialog.js#L723)）。

### 4.6 Phase 1 总验收

- [ ] schema v2 落地 + 旧数据兼容测试
- [ ] 批 A 4 种接线完成（含 P-3 修复）
- [ ] 批 B 7 种双路径完成（含无解预检）
- [ ] 批 C 编译器 + 6 种语法糖
- [ ] 前端 `BACKEND_RULE_TYPES`、`RULE_TYPE_LABELS`、`requirementIntentLabel` 词典同步扩容
- [ ] fulfillment 对全部新类型可评估（详见 Phase 4，可先留 TODO 桩但接口要留好）
- [ ] `npm test` + `solver` 侧测试全绿

---

## 5. Phase 2 — AI 语义理解引擎（AI-first）

> **目标**：从"本地正则为主、AI 复审为辅"升级为"AI 结构化抽取为主、本地正则兜底"，让几乎所有中文排课表达都能被理解或触发澄清。
> **建议 change id**：`ai-first-requirement-extraction`

### 5.1 架构

```
用户输入文本
   │
   ├─ (1) 预处理：splitSentences/splitClauses（已有，复用）
   │
   ├─ (2) AI 结构化抽取 extractRequirementsWithAI()   ← 本 Phase 新建
   │       输入：句子组 + 项目实体表 + 约束目录
   │       输出：意图层 requirement[]（含 confidence / clarify）
   │       模型：resolveAiConfig() 现有 DeepSeek 配置（timetable-rule-parser.js:221）
   │       response_format: { type: "json_object" }
   │
   ├─ (3) 实体对齐 resolveEntityRefs()                ← 新建
   │       名字 → id；歧义/未知 → clarification
   │
   ├─ (4) 意图编译 compileRequirementToRows()          ← Phase 1 已建
   │
   ├─ (5) 本地校验 validateCompiledRows()              ← 新建
   │       参数完整性 / slot 越界 / detectRuleConflicts 预检（已有）
   │
   └─ (6) 组装 buildRuleReviewResult()（已有）→ 复核台
 
降级链：AI 超时/未配置/解析失败 → 现有本地正则管线原样运行（不删任何现有正则代码！）
缓存：复用 PARSE_CACHE（timetable-rule-parser.js:25），key 已含 env 配置指纹
```

**开关**：环境变量 `TIMETABLE_RULE_AI_EXTRACT=1` 启用 AI-first（默认关，灰度用）；沿用 `TIMETABLE_RULE_AI_REVIEW_DISABLED`、`TIMETABLE_RULE_AI_REVIEW_TIMEOUT_MS`（[timetable-rule-parser.js:118-126](gateway/services/timetable-rule-parser.js#L118)）。

### 5.2 完整 Prompt 模板（直接可用，放 `gateway/services/timetable-ai-prompts.js` 新文件）

````javascript
export function buildExtractionPrompt(project, text) {
    return `你是"智能排课约束理解引擎"。任务：把用户的中文排课需求精确转换为结构化 JSON。只输出 JSON，不要任何解释文字。

【学校时间模型】
- 上课日：${activeWeekdaysLabel(project)}（1=周一 … 7=周日）
- 每天节次：第 1 ~ ${periodsPerDay(project)} 节
- 上午节次：${morningPeriods(project).join('、')}；下午节次：${afternoonPeriods(project).join('、')}
- 时间槽写法："天-节"，如 "1-3" = 周一第 3 节

【实体表】只能引用这里出现的名字，禁止编造：
教师：${project.teachers.map(t => t.name).join('、')}
班级：${project.classes.map(c => c.name).join('、')}
科目：${project.subjects.map(s => s.name).join('、')}

【约束类型目录】intent 必须取自下表：
teacher_unavailable 教师不可排 | class_unavailable 班级不可排 | global_unavailable 全校不可排
locked_slot 固定课节 | first_period_assign 首节固定 | teaching_group_meeting 教研活动
subject_morning 上午优先 | subject_afternoon 下午优先 | subject_preferred_periods 优先节次
subject_avoid_periods 避开节次 | avoid_first_period 避开第一节 | avoid_last_period 避开最后一节
subject_spread 课程分散 | course_interval 同科间隔 | subject_daily_limit 科目每日上限
class_daily_subject_balance 班级每日均衡 | teacher_daily_limit 教师每日上限
teacher_consecutive_limit 教师连续上限 | teacher_weekly_limit 教师每周上限
teacher_max_days_per_week 每周最多上课天数 | teacher_gap_preference 教师少空堂
teacher_load_balance 教师负载均衡 | teacher_mutual_exclusion 教师互斥
subject_not_same_day 科目不同天 | subject_sequence 科目先后 | room_requirement 教室要求
block_preference 连堂设置 | lunch_protection 午休保护 | week_pattern 单双周
campus_commute 跨校区间隔

【输出 JSON Schema】
{
  "requirements": [
    {
      "intent": "目录中的 intent",
      "targetType": "teacher|class|subject|teaching_group|global",
      "targetNames": ["从实体表原样抄写的名字"],
      "time": { "days": [1,3], "periods": [1,2], "dayPart": "morning|afternoon|null", "slots": ["1-1"] },
      "params": { "limit": 2, "blockPreference": "double", "minGapDays": 1, "pairNames": [] },
      "strength": "hard|soft",
      "confidence": 0.95,
      "needsClarification": false,
      "clarify": { "question": "", "field": "", "kind": "number|choice|text", "options": [] },
      "evidence": "触发本条的原文片段"
    }
  ],
  "unrecognized": [ { "text": "无法归类的原文", "reason": "原因" } ]
}

【判定规则】
1. "不排/不上/不要排/请假/没空/有事/开会/外出" → unavailable 类，strength=hard
2. "必须/固定/锁定/一定/只能" → hard；"尽量/优先/最好/希望/建议" → soft
3. 时间只给"上午/下午"没给节次 → time.dayPart 填值，slots 留空（由系统展开）
4. 人名不在实体表 → 该条 needsClarification=true，clarify.kind="choice"，options 填实体表中最相近的 1-3 个名字
5. 数值必需但缺失（如"别太多"没说几节）→ needsClarification=true，clarify.kind="number"，question 里给建议默认值
6. 一句话含多个需求 → 拆成多条 requirement
7. 与排课无关的内容 → 放 unrecognized
8. confidence：表述明确且实体唯一命中=0.9+；有推断=0.6~0.9；含糊=0.6 以下

【示例】
输入：张伟老师周一上午和周三下午都不排课
输出：{"requirements":[{"intent":"teacher_unavailable","targetType":"teacher","targetNames":["张伟"],"time":{"days":[1],"periods":[],"dayPart":"morning","slots":[]},"params":{},"strength":"hard","confidence":0.96,"needsClarification":false,"clarify":null,"evidence":"张伟老师周一上午"},{"intent":"teacher_unavailable","targetType":"teacher","targetNames":["张伟"],"time":{"days":[3],"periods":[],"dayPart":"afternoon","slots":[]},"params":{},"strength":"hard","confidence":0.96,"needsClarification":false,"clarify":null,"evidence":"周三下午都不排课"}],"unrecognized":[]}

输入：数学老师们连堂别超过三节，另外物理和化学的实验课别放同一天
输出：{"requirements":[{"intent":"teacher_consecutive_limit","targetType":"teaching_group","targetNames":["数学"],"time":{},"params":{"limit":3},"strength":"soft","confidence":0.85,"needsClarification":false,"clarify":null,"evidence":"数学老师们连堂别超过三节"},{"intent":"subject_not_same_day","targetType":"subject","targetNames":["物理","化学"],"time":{},"params":{"pairNames":["物理","化学"]},"strength":"soft","confidence":0.82,"needsClarification":false,"clarify":null,"evidence":"物理和化学的实验课别放同一天"}],"unrecognized":[]}

输入：老师的课别太密
输出：{"requirements":[{"intent":"teacher_consecutive_limit","targetType":"teacher","targetNames":[],"time":{},"params":{},"strength":"soft","confidence":0.5,"needsClarification":true,"clarify":{"question":"「别太密」是指教师连续上课不超过几节？常用值为 3。","field":"limit","kind":"number","options":[]},"evidence":"老师的课别太密"}],"unrecognized":[]}

现在处理用户输入：
${text}`;
}
````

> 实施注意：few-shot 至少扩到 10 例（补：locked_slot、subject_afternoon、teaching_group_meeting、room_requirement、week_pattern、mutual_exclusion、纯闲聊进 unrecognized 各 1 例）。示例必须和目录 intent 完全一致，否则模型会自造类型。

### 5.3 抽取函数骨架

**改动文件**：`gateway/services/timetable-rule-parser.js`（或拆新文件 `timetable-ai-extractor.js`，由 rule-parser import）

```javascript
async function extractRequirementsWithAI({ project, text, env, fetchImpl }) {
    const { apiKey, baseUrl, model } = resolveAiConfig(env);          // 已有:221
    const fetchClient = resolveFetch(fetchImpl);
    const response = await fetchAiReviewWithTimeout(fetchClient,      // 已有:3779（复用超时封装）
        `${baseUrl}/chat/completions`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${apiKey}` },
            body: JSON.stringify({
                model,
                messages: [{ role: 'user', content: buildExtractionPrompt(project, text) }],
                response_format: { type: 'json_object' },
                temperature: 0,                                       // 抽取任务必须 0
            }),
        }, aiReviewTimeoutMs(env));
    const payload = await response.json();
    const parsed = safeJsonParse(payload?.choices?.[0]?.message?.content);
    return validateExtractionPayload(parsed);   // 新建：schema 校验，intent 不在目录→丢进 unrecognized
}
```

`parseAiOrLocal`（[timetable-rule-parser.js:4797](gateway/services/timetable-rule-parser.js#L4797)）里加分支：`TIMETABLE_RULE_AI_EXTRACT=1` 且输入是自由文本时先走 `extractRequirementsWithAI`，成功则其结果作为 `semanticRequirements` 喂给现有 `buildRequirementSemantics`；失败/超时 fallback 现有链路。**Excel 路径维持"本地结构化优先 + AI 复审"不变**（表格本来就是结构化的，AI-first 反而引入噪声）。

### 5.4 实体对齐 `resolveEntityRefs()`

```javascript
function resolveEntityRefs(requirement, project) {
    // 逐级匹配，命中即停：
    // 1. 全等匹配 name === ref
    // 2. 去称谓匹配（去掉"老师/教师/组"后全等）
    // 3. 包含匹配且唯一（"张伟" 命中 "张伟华" 仅当只有一个包含者）
    // 4. 教研组展开：targetType==='teaching_group' → 该科目全部任课教师
    // 结果三态：
    //   matched   → matchedIds 填入，confidence 不动
    //   ambiguous → needsClarification=true, clarify.kind='choice', options=候选名
    //   missing   → needsClarification=true, clarify.kind='choice', options=编辑距离最近的≤3个
}
```

**规矩**：**永远不猜**。多候选或零候选一律转澄清，宁可多问一句，不可错配一个（错配教师不可排=把别人的课锁死，市场事故）。

### 5.5 置信度 → 状态策略

| confidence | needsClarification | 进复核台的状态 |
|---|---|---|
| ≥ 0.9 | false | `actionable`（可直接应用） |
| 0.6 ~ 0.9 | false | `actionable` 但行上加"AI 推断"徽章，详情面板展示 evidence 原文 |
| 任意 | true | `needs_review` + clarification 表单 |
| < 0.6 | false | `needs_review`，warning="置信度低，请人工确认" |

### 5.6 黄金语料库（回归测试的地基）

**新建**：`test/fixtures/constraint-corpus.jsonl`——每行一条：

```json
{"text":"张伟老师周一上午不排课","expect":{"intent":"teacher_unavailable","targetNames":["张伟"],"days":[1],"dayPart":"morning","strength":"hard"}}
{"text":"体育别放第一节","expect":{"intent":"avoid_first_period","targetNames":["体育"],"strength":"soft"}}
{"text":"老师课时要均衡","expect":{"intent":"teacher_load_balance","strength":"soft"}}
{"text":"帮我看看明天天气","expect":{"unrecognized":true}}
```

初始 ≥ 100 句，构成：每种意图 ≥ 2 句常规 + 1 句口语变体；10 句需澄清；5 句无关内容；10 句多需求复合句。来源：`AI排课约束建议.xlsx` 全部行 + 参考 FET/水晶排课的约束文档措辞（附录 D）+ 自造口语变体。

**新建**：`test/timetable-ai-extraction.test.js`——AI 可用时全量跑语料断言（CI 无 key 时 skip，本地/灰度环境跑）；实体对齐、语法糖编译、置信度映射是纯函数，**无需 AI 即可全量单测**。

### Phase 2 验收

- [ ] `TIMETABLE_RULE_AI_EXTRACT=1` 下自由文本走 AI-first，关掉后行为与现在完全一致
- [ ] 语料 ≥ 100 句，覆盖率 ≥ 95%、字段准确率 ≥ 98%（记录一份基线报告存 `test/fixtures/corpus-baseline.md`）
- [ ] 实体不猜：歧义/未知 100% 转澄清（单测覆盖）
- [ ] AI 超时/未配置降级链路测试（mock fetch 超时）
- [ ] 解析耗时：单次输入（≤10 句）P95 ≤ 15s（含 AI 调用）

---

## 6. Phase 3 — 多轮澄清对话

> **目标**：把"单字段一问一答"升级为可多轮、带快捷选项的对话式澄清，压平模糊输入的落地门槛。
> **建议 change id**：`clarification-dialogue`
> **现状基础**：`item.clarification{question,field,kind,options,min,max}` 结构已有；`/requirements/clarify` 端点已有（[timetable.js:648](gateway/routes/timetable.js#L648)）；前端 `renderRequirementClarification` 已有（[view-constraint-dialog.js:762](public/js/tools/timetable/view-constraint-dialog.js#L762)）但只渲染 number/text 输入框，**options 没渲染**。

### 任务 3.1 选项快捷回复（最小改动，先做）

`renderRequirementClarification` 里 `kind === 'choice'` 时渲染 chips：

```html
<div class="tt-clarify-options">
  ${options.map(opt => `
    <button class="tt-example-chip" data-action="submit-requirement-clarification"
        data-requirement-id="${id}" data-clarify-value="${opt}" type="button">${opt}</button>`).join('')}
</div>
```

`submitRequirementClarification`（[controller-constraint-dialog.js:344](public/js/tools/timetable/controller-constraint-dialog.js#L344)）支持从 `data-clarify-value` 直接取值（有值则跳过 input 读取）。

### 任务 3.2 多轮会话

后端 `continueTimetableRequirementClarification`（被 `/requirements/clarify` 调用）返回结果里，若该 requirement 补完一个字段后仍缺参数，则生成下一个 clarification（而不是清空）。会话状态就存在 requirement 自身（无需新会话存储）：

```javascript
requirement.clarificationHistory = [
    { question, answer, at },   // 每轮追加
];
requirement.clarification = nextQuestionOrNull;
```

### 任务 3.3 澄清策略表（写死在代码里的标准问题）

**新建** `gateway/services/timetable-clarify-policies.js`：每种意图缺哪个参数问什么、默认值多少：

| intent | 缺参 | 标准问题 | kind | 默认 |
|---|---|---|---|---|
| teacher_consecutive_limit | limit | "连续上课最多几节？常用 3" | number | 3 |
| subject_daily_limit | limit | "同一天最多几节？常用 2" | number | 2 |
| teacher_unavailable | slots/dayPart | "具体哪些时间不排？" | choice(上午/下午/整天/具体节次) | — |
| room_requirement | roomIds | "用哪间教室？" | choice(项目教室列表) | — |
| * | targetNames 歧义 | "你说的是哪一位？" | choice(候选) | — |

### 任务 3.4 详情面板对话化 UI

详情面板"待补充信息"区改为气泡流（历史轮次灰底、当前问题白底 + 输入/chips），示意见 §10.2-C。

### Phase 3 验收

- [ ] choice 类澄清可点选直达
- [ ] 多轮：构造"老师别太累"→ 问连续上限 → 答 3 → 问是否也限每日 → 答 5 → 两条规则落地
- [ ] 全部澄清路径不刷新弹窗、不丢已选筛选（保留 `selectedRequirementId`，现有逻辑已做，回归即可）
- [ ] 单测：clarify 两轮流程 mock 接口断言状态迁移

---

## 7. Phase 4 — 求解反馈闭环

> **目标**：排完课，逐条回答"你的每一条要求，满足了没有；没满足的，为什么 + 怎么办"。
> **建议 change id**：`fulfillment-loop`
> **现状基础**：`/rules/fulfillment` 端点与 `timetable-constraint-fulfillment.js` 已有（最近一次提交刚增强过）；`/rules/diagnose` 已有；前端 `controller.js:749` 已调用 fulfillment。

### 任务 4.1 满足度报告数据结构 v2

`evaluateTimetableConstraintFulfillment`（fulfillment 服务）输出补齐为：

```javascript
{
  summary: { total, satisfied, partiallySatisfied, violated, notEvaluable },
  items: [{
    ruleId, type, typeLabel, origin,                 // Phase 0 的 origin 贯通到这里
    strength: 'hard'|'soft',
    status: 'satisfied'|'partial'|'violated'|'not_evaluable',
    detail: '周一第3节 三(2)班 数学 违反了「张伟周一上午不排课」',
    evidenceSlots: [{ day, period, classId, subjectId, teacherId }],  // 可点击跳转课表格子
    suggestions: [{ kind: 'relax_to_soft'|'shrink_slots'|'delete_rule'|'manual', label: '改为软约束' }],
  }],
}
```

新原语（Phase 1 的 22 种）每种都要有评估函数——评估就是"拿 schedule.slots 对着规则数一遍"，纯函数，不难但要全。

### 任务 4.2 报告 UI（排课完成后自动弹出/侧栏入口）

见 §10.2-D 布局。关键交互：
- 违反项点击 → 课表对应格子高亮（复用现有课表渲染的定位机制）。
- 每个违反项挂 1-3 个处置按钮：改软约束 / 缩小时间范围 / 删除该规则 → 调用现有规则编辑/删除动作 → 底部出现"重新排课"按钮。
- 与 `/rules/diagnose` 的建议合并展示（diagnose 给"无解风险"，fulfillment 给"已解但违反"）。

### 任务 4.3 应用后立即预检提示

`applyConstraintsFromDialog` 应用成功后立刻调 `/rules/diagnose`，有 blocking 冲突时在成功提示后追加："注意：检测到 1 处可能无解的冲突（李老师每周上限 16 节 < 任课 18 节），建议先处理再排课。"

### Phase 4 验收

- [ ] 22 种原语全部可评估（含 not_evaluable 的诚实标注，如本地路径的教室约束）
- [ ] 构造"违反 2 条软约束"的项目，报告准确列出 2 条并可点击跳转
- [ ] 处置动作 → 重排 → 报告变绿 的完整手工冒烟录屏一次（留存 artifacts/）
- [ ] `test/timetable-scheduler.test.js` 加 fulfillment 断言用例

---

## 8. Phase 5 — 端到端 AI Agent

> **目标**：单一对话入口完成"说需求 → 反问澄清 → 确认应用 → 触发排课 → 汇报结果"全流程。
> **建议 change id**：`constraint-agent`
> **现状基础**：`gateway/services/timetable-agent/skills/solve-plan-skill.js` 已有 agent 骨架；前端 `renderAIChatPanel` + `start-ai-chat` 已有聊天面板。

### 任务 5.1 新增 skill：`constraint-intake-skill.js`

放 `gateway/services/timetable-agent/skills/`，职责编排（不重写逻辑，全部调用已有函数）：

```
状态机：INTAKE → CLARIFY(循环) → CONFIRM → APPLY → SOLVE → REPORT
INTAKE  : parseTimetableRules / extractRequirementsWithAI
CLARIFY : clarify policies（Phase 3），最多 3 轮，超出转人工复核台
CONFIRM : 生成变更摘要（N 硬 M 软 K 计划），等用户"确认"
APPLY   : /rules/normalize + /rules + /requirements/apply 同款服务函数
SOLVE   : 现有排课入口（controller 侧触发或后端 solve 服务）
REPORT  : evaluateTimetableConstraintFulfillment → 自然语言摘要
```

**安全边界（必须遵守）**：
- APPLY 前必须有用户显式确认消息，agent 不得自行应用。
- 硬约束的删除/放宽必须单独列出让用户点头。
- 每轮 agent 回复末尾带状态行：`[已理解 5 · 待澄清 1 · 待确认 0]`。

### 任务 5.2 前端聊天入口

现有约束弹窗的"AI 优化"按钮旁加"对话排课"模式：聊天流里内嵌迷你复核卡（每条已理解需求一张小卡：意图+对象+参数+`[改][删]`），确认后走同一套 apply。**复核台不删**——专业用户仍可切表格视图，两个视图共享同一份 `ruleReview` 状态。

### 任务 5.3 演示剧本（市场 demo 用，写进测试）

```
用户：三年级下学期，语文数学英语是主科尽量上午，体育音乐美术下午，
      张伟周一全天不在，每天第一节全校早读不排常规课，老师连堂别超3节。
Agent：已理解 5 类需求（明细卡×5）。「全校早读」我按周一到周五第1节全校不可排处理，对吗？
用户：对。
Agent：确认应用？3 条硬规则 + 2 条软规则。
用户：确认。
Agent：已应用。开始排课…完成。34/35 条约束满足；1 条软约束部分满足：
      周四三(2)班音乐在上午（下午没有空位），需要我尝试调整吗？
```

### Phase 5 验收

- [ ] 剧本全流程真实跑通（真实 AI + 真实求解）
- [ ] agent 不确认不应用（单测：状态机 CONFIRM 前调 APPLY 抛错）
- [ ] 聊天与复核台状态一致（聊天里改的参数，切到表格视图同步可见）

---

## 9. Phase 6 — 市场化硬化

> **建议 change id**：`market-hardening`

### 9.1 性能与成本

| 项 | 做法 |
|---|---|
| AI 调用缓存 | 沿用 PARSE_CACHE；语料级 prompt 稳定（temperature 0 + seed，env `TIMETABLE_RULE_AI_SEED` 已有） |
| 长文本分批 | >30 句拆批并行抽取，合并后统一 dedupe（`dedupeRequirements` 已有） |
| token 预算 | 实体表超 200 项时只注入被句子提到的候选（先本地粗筛名字再进 prompt） |
| 求解超时分级 | 沿用 `TIMETABLE_SOLVER_TIMEOUT`；30 班以上默认提高到 300s 并在 UI 提示预计时长 |

### 9.2 可观测性

结构化日志统一字段（后端 logger）：`parse_id, input_type, sentence_count, ai_used, ai_ms, extracted_count, clarify_count, apply_count, solve_ms, hard_violations, soft_violations`。
新增内部计数端点或日志聚合脚本（`scripts/`）产出周报：解析成功率、澄清率、应用率、求解成功率——这四个数就是产品健康度仪表盘。
失败样本采集：AI 抽取失败/用户放弃的输入脱敏后写 `logs/constraint-miss.jsonl`，每周人工过一遍 → 变成新语料/新意图。

### 9.3 发布检查单（每次对外版本）

- [ ] 黄金语料回归 ≥ 基线（不允许倒退）
- [ ] 三个旧项目数据文件（找真实历史 data/）加载 + 排课无回归
- [ ] AI 断网降级演练：拔 key 全流程可用（本地正则路径）
- [ ] 100 班压力项目求解不 OOM、前端不卡死
- [ ] 所有 alert()/confirm() 文案过一遍中文表达评审

### 9.4 试点与迭代

1. 内部试点：用 2-3 套真实学校数据（脱敏）完整排一学期课表，教务处角色演练。
2. 语料回流：试点期所有真实输入进语料库（授权+脱敏），每两周一轮"新语料 → 新意图 → 编译器/原语扩展"迭代。
3. 模板市场：把常见学校的整套约束做成一键导入模板（"小学标准模板：早读+午休+主科上午+体育下午+……"），降低冷启动成本。

### 9.5 用户文档

- 《你可以这样说》输入示例卡（按 30 种意图各给 2 句，就放弹窗"常用示例"区扩容）
- Excel 模板下载（表头即字段说明）
- FAQ：为什么需要澄清 / 硬软约束区别 / 无解了怎么办

---

## 10. UI 设计总规范

### 10.1 设计原则

1. **说人话**：所有状态、去向、原因用教务老师能懂的话（词典见 Phase 0 任务 0.4/0.6）。
2. **看得见去向**：每条需求任何时刻都能回答"它现在在哪、接下来会去哪"。
3. **来源可溯**：任何一条规则都能看到触发它的原文/文件行。
4. **可撤销**：应用前可暂停单条（已有 toggle-constraint-apply-item），应用后可在规则管理删除。
5. **诚实降级**：AI 不可用、本地引擎不支持某约束，都明说，不装。

### 10.2 界面布局

**A. 约束助手主弹窗（Phase 0 改版后）**

```
┌──────────────────────────────────────────────────────────────────┐
│ 🧠 智能约束助手                                    [AI优化] [✕]   │
│ 把自然语言排课需求转换为可复核、可应用的规则                        │
├──────────────────────────────────────────────────────────────────┤
│ 当前进度  3/4  请检查解析结果          ①输入 ②理解 ③复核 ④应用   │
│ 规则来源  [对话输入] [上传文件] [手动填写]                         │
│ ┌────────────────────────────────────────────────────────────┐  │
│ │ (输入区，识别后压缩)                                         │  │
│ └────────────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────┤
│ 解析结果                                          [清空全部]      │
│ 来自你的输入 17 条 · 系统补充 69 条 · 本次可写入排课 17 条         │
│ [✅可应用17] [⚠待确认0] [✓系统内置69] [🧩复杂模型1]               │
│ 筛选：[全部] [约束规则] [任课计划] [优化目标] [系统内置] [待确认]   │
│ ┌───────────────────────────────┬────────────────────────────┐  │
│ │ 状态   对象  需求   去向  来源  │  详情面板                   │  │
│ │ 可应用 体育  避开第一节         │  [可应用] 体育               │  │
│ │        → 排课规则  文件第3行    │  需求：避开节次              │  │
│ │ 可应用 张伟  周一不排           │  去向：→ 排课规则            │  │
│ │        → 排课规则  文件第4行    │   "应用后写入项目规则，       │  │
│ │ ▸ 系统补充的默认规则(69) [展开] │    下次排课必须遵守"          │  │
│ │                               │  参数：周一第1节…            │  │
│ │                               │  原文："体育避开第一节"       │  │
│ │                               │  落地结果：规则草稿 ×1        │  │
│ └───────────────────────────────┴────────────────────────────┘  │
├──────────────────────────────────────────────────────────────────┤
│                    [取消]  [✓ 应用需求 (17)]                      │
│                            将写入排课规则，立即参与下次排课         │
└──────────────────────────────────────────────────────────────────┘
```

**B. 应用确认弹窗（Phase 0 任务 0.5）**——分类明细 + 生效说明（文案见任务 0.5）。

**C. 澄清对话区（Phase 3，嵌在详情面板）**

```
┌ 待补充信息 ─────────────────────────────┐
│ 🤖 「别太密」是指连续上课不超过几节？     │
│    [ 3节 ] [ 4节 ] [ 自定义… ]           │
│ ─ 历史 ─                                │
│ 🤖 你说的是哪位张老师？  你：张伟         │
└─────────────────────────────────────────┘
```

**D. 求解满足度报告（Phase 4）**

```
┌ 排课完成 · 约束满足报告 ────────────────────────────┐
│ ✅ 满足 32   ⚠️ 部分 2   ❌ 违反 1   ➖ 未评估 1     │
├────────────────────────────────────────────────────┤
│ ❌ [硬] 张伟周一上午不排课        我的输入·文件第4行 │
│    周一第2节 三(2)班数学 仍安排了张伟 → [点击定位]   │
│    处理：[改为软约束] [缩小到第1-2节] [删除此规则]   │
│ ⚠️ [软] 体育尽量下午             我的输入           │
│    周四三(1)班体育在上午（下午无空位）               │
├────────────────────────────────────────────────────┤
│                [导出报告]  [调整后重新排课]          │
└────────────────────────────────────────────────────┘
```

**E. Agent 对话（Phase 5）**：标准聊天流 + 迷你需求卡 + 状态行，见任务 5.2/5.3。

### 10.3 标准文案表（直接抄）

| 场景 | 文案 |
|---|---|
| 解析中 | 正在本地识别需求… / 正在让 AI 理解你的需求… / 正在生成审核台… |
| AI 降级 | AI 暂时不可用，已用本地识别；复杂表达可能没认全，建议逐条检查。 |
| 空结果 | 没有识别到排课需求。试试："张老师周一上午不排课"。 |
| 应用成功 | 已写入 N 条硬规则、M 条软规则，更新 K 个任课计划。下次排课立即生效。 |
| 无解预警 | 检测到可能无解的冲突：{明细}。建议先处理再排课。 |
| 不支持 | 这条需求当前版本无法自动实现，已记录为人工调课参考。 |

### 10.4 状态色 tone（沿用现有 tt- 类）

`actionable`→绿（现有 actionable tone）/ `review`→黄（warning）/ `handled`→灰 / `violated`→红 / 复杂模型→紫（is-complex 已有）。不新增颜色系统。

---

## 11. 测试与验收总表

| Phase | 自动化测试 | 手工冒烟 |
|---|---|---|
| 0 | planner-ui：双计数/折叠/文案断言 | 上传 xlsx 全流程截图对比 |
| 1 | schema 归一化；每原语解析→存储→双路径求解端到端；无解预检 | 6 班小项目逐约束验证课表 |
| 2 | 语料回归（AI 环境）；实体对齐/编译器/置信度纯函数单测；降级链路 | 10 句口语随机输入体验 |
| 3 | clarify 两轮状态迁移；choice 提交 | "别太密"完整对话 |
| 4 | 22 种 fulfillment 评估单测；违反定位数据正确性 | 报告→处置→重排→变绿 |
| 5 | 状态机安全（未确认不应用）；剧本 e2e | 市场 demo 剧本演练 |
| 6 | 发布检查单全项 | 试点数据全流程 |

全局回归命令：`npm test`；求解器：`cd solver && ./mvnw.cmd test`。
**纪律**：任何"已验证"都必须真实跑过，失败要写现象+复现+下一步（仓库规则）。

---

## 12. 实施顺序、依赖与风险

### 12.1 依赖图

```
Phase 0（独立，最先）
   │
Phase 1 批A ──▶ 批B ──▶ 批C（编译器）
   │                        │
   └────────────┬───────────┘
                ▼
            Phase 2（AI-first，依赖 1 的原语与编译器）
                ▼
            Phase 3（澄清，依赖 2 的 clarify 产出）
                ▼
            Phase 4（反馈，依赖 1 的全类型评估；可与 3 并行开工）
                ▼
            Phase 5（Agent，编排 2+3+4）
                ▼
            Phase 6（硬化，贯穿收尾）
```

可并行：Phase 0 与 Phase 1 批 A；Phase 3 与 Phase 4。

### 12.2 风险与对策

| 风险 | 对策 |
|---|---|
| AI 输出不稳定/幻觉实体 | temperature 0 + seed；实体白名单校验（5.3 validateExtractionPayload：intent 不在目录、名字不在实体表一律降级/转澄清）；永不直接生效低置信项 |
| 硬约束过强导致无解 | B-3 式预检前移到应用时；Phase 4 报告给处置动作；文案引导"拿不准就选软约束" |
| 旧项目数据兼容 | schema 只增不改；发布检查单固定回归 3 份历史数据 |
| 本地/Timefold 行为不一致 | 每原语在 4.2 清单里显式写两条路径行为；不一致处 UI 诚实标注（如教室约束） |
| AI 成本失控 | 缓存 + 分批 + 实体粗筛（9.1）；日志统计 ai_ms/调用次数周报 |
| 范围蔓延 | 新"说法"一律先问能否进编译器（2.1 规矩），原语扩容必须走黄金清单十步 |

### 12.3 与仓库流程对接

每个 Phase 对应一个 OpenSpec change（id 建议已写在各 Phase 标题下），proposal 引用本文档相应章节即可，批准后实施。本文档本身是路线图，不替代逐 change 的审批。

---

## 附录 A：约束类型 ↔ 代码位置速查表

| 原语 type | 存储（project.rules） | bridge 传递 | Java 约束 | 本地路径 | 状态 |
|---|---|---|---|---|---|
| teacher_unavailable | hardRules.teacherUnavailable | blockedTimeSlotIds | blockedTime | 放置过滤 | ✅ |
| class_unavailable | hardRules.classUnavailable | blockedTimeSlotIds | blockedTime | 放置过滤 | ✅ |
| locked_slot | hardRules.lockedSlots | pinnedTimeSlotId | pinnedTime | 预放置 | ✅ |
| global_unavailable | hardRules.globalUnavailable | blockedTimeSlotIds（并入） | blockedTime | 放置过滤 | 🆕 B-1 |
| subject_morning | softRules.morningSubjects | preferMorning | mainSubjectsEarlier | 评分 | ✅ |
| subject_afternoon | softRules.afternoonSubjects | preferLater | practicalSubjectsLater | 评分 | 🔌 A-1 |
| subject_preferred/avoid_periods | softRules（现实现） | （经现有软规则通道） | —/评分 | 评分 | ✅ |
| subject_spread(+minGap) | softRules | spreadMinGapDays | spreadSameCourse | 评分 | ✅/🍬 B-7 |
| subject_daily_limit | hardRules.subjectDailyLimit | subjectDailyMax | subjectDailyLimit(新) | 放置过滤 | 🆕 B-2 |
| class_daily_subject_balance | softRules.classDailyBalance | classMainDailyMax | classDailyLoad+新 | 评分 | 🔌 A-3 |
| teacher_daily/consecutive_limit | softRules.teacherLimits | （现有） | teacherDailyLoad/ContinuousTeachingLimit | 评分 | ✅ |
| teacher_weekly_limit | hardRules.teacherWeeklyLimit | teacherWeeklyMax | 新 groupBy | 放置过滤 | 🆕 B-3 |
| teacher_max_days_per_week | hardRules.teacherMaxDaysPerWeek | teacherMaxDays | 新 countDistinct | 放置过滤 | 🆕 B-3 |
| teacher_gap_preference | softRules.teacherGapWeight | solverWeights.teacherGap | teacherGap | 评分 | 🔌 A-4 |
| teacher_load_balance | softRules.teacherLoadBalance | solverWeights.teacherLoadBalance | LoadVarianceMinimization | 评分 | 🔌 A-4 |
| teacher_mutual_exclusion | hardRules.teacherMutualExclusion | mutualExclusionGroups | 新 pairwise | 放置过滤 | 🆕 B-4 |
| subject_not_same_day | hardRules.subjectNotSameDay | notSameDaySubjectIds | 新 pairwise | 过滤/评分 | 🆕 B-5 |
| subject_sequence | softRules.subjectSequence | sequencePartners | 新 pairwise 软 | 评分 | 🆕 B-6 |
| room_requirement | hardRules.roomRequirements | allowedRoomIds（并入） | roomRequirement/roomConflict | 诚实降级 | 🔌 A-2 |
| week_pattern | complex 模型字段（已有骨架） | weekPattern | — | complex | 🧩 P6 |
| campus_commute | commuteRules（已有骨架） | — | — | complex | 🧩 P6 |
| 语法糖 6 种 | —（编译到上表） | — | — | — | 🍬 4.5 |

## 附录 B：API 契约汇总

**现有（不变）**：
- `POST /api/tools/timetable/rules/parse`（multipart：text/file/project）→ `{ draftRules, draftRows[], requirementItems[], semanticActions[], autoAcceptable[], needReview[], clarifyingQuestions[], missingInfo[], conflicts[], warnings[], unsupportedItems[], ruleReport, confidenceSummary, nextAction, source, parseSource, parserVersion, cacheHit, aiReview, inputType, contextStats }`
- `POST /rules/normalize`（draftRows[]）→ 同形状（校验后）
- `POST /rules`（rules）→ `{ project }`
- `POST /requirements/apply`（actions[]）→ `{ project, applied[], needsReview[] }`
- `POST /requirements/clarify`（previousResult, answers[]）→ 解析结果形状
- `POST /rules/diagnose` / `POST /rules/fulfillment`

**Phase 变更**：
- P0：requirementItem 增 `origin`
- P2：parse 内部增 AI-first 分支（契约不变，`parseSource` 增枚举 `ai_extract`）
- P3：clarify 响应内 requirement 增 `clarificationHistory[]`
- P4：fulfillment 响应升级为 §7 任务 4.1 结构（新增字段，不删旧字段）

## 附录 C：术语表

| 术语 | 含义 |
|---|---|
| 意图 intent | 用户想要什么（自然语言层的分类，~30 种，可扩展） |
| 原语 primitive | project.rules 里的规范存储字段对应的规则类型（~22 种，稳定） |
| 编译 compile | 意图 → 原语 draftRow 的转换（timetable-intent-compiler.js） |
| draftRow | 一条待应用的机器规则草稿 |
| requirementItem | 复核台上的一条"需求"（可挂多条 draftRow 和语义动作） |
| 语义动作 semanticAction | 非规则类落地（改任课计划/优化目标/复杂模型） |
| 双路径 | 本地启发式求解 + Timefold 求解，两者都要遵守规则 |
| 黄金语料 | 带期望输出的测试句子集，回归的基准 |

## 附录 D：参考资料

- 本地参考仓库①：`c:\Users\ICe\Desktop\排课参考1-水晶排课源码`（国产排课约束措辞与功能清单参考）
- 本地参考仓库②：`c:\Users\ICe\Desktop\排课参考2-fet源码`（FET 拥有约 150 种时间/空间约束，是约束目录完备性的对标基准；本方案 30 意图 ≈ 覆盖 FET 中学校景高频子集）
- 现有测试样例：`AI排课约束建议.xlsx`（根目录，作为语料库第一批来源）
- V2 吸收笔记：`reference/timetable-improvement-notes.md`（诊断/导入报告/规则解释相关，Phase 4 可参考）

---

*本文档到此为止。按 Phase 0 开始动手。*
