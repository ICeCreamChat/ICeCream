# 座位规划器两项改进：NLP 描述词误识别 + 桌面图标布局

## 问题背景

### 问题一：「高个子」等描述词被误识别为人名

当学生需求中包含「不想坐在高个子后面」时，系统的 `avoid_behind` 约束解析逻辑会将「高个子」捕获为 `related` 人名。这是因为：

1. **本地解析器**（`parseSeatingConstraintsLocally`）使用正则 `/不想坐在(.+?)后面/` 提取 `related`，捕获组会贪婪匹配到「高个子」
2. `splitNameList()` 会先尝试在花名册中匹配，匹配不到就按分隔符拆分并原样返回，**不会过滤描述性词汇**
3. **AI 解析器**（DeepSeek）的 system prompt 中也缺少对「非人名描述词」的排除指引，可能将「高个子」作为 `related` 返回
4. `looksLikeRowOrPlaceholder()` 的黑名单只包含位置/成绩类词汇，没有涵盖身体特征描述词（高个子、矮个子、近视等）

**影响**：生成无效的 `avoid_behind` 约束（related = "高个子"），这个约束无法匹配任何学生 ID，在排座时不会报错但也不会生效，同时占用约束配额。

### 问题二：桌面图标位置不合理

当前课桌上的状态图标（🍬糖果、👓眼镜、📚书本、⚠️警告）：
- 使用 `justify-content: space-between` 横向分散排列在桌面底部
- 多个图标时水平排列挤在一起
- 视觉上不够整洁

用户期望：
- 图标统一放在课桌**右下角**
- 多个图标时**上下垂直排列**

---

## 提议的修改

### 组件一：NLP 描述词过滤（避免误识别）

#### [MODIFY] [seating-constraints.js](file:///d:/607document/ICeCream/gateway/services/seating-constraints.js)

**改动 1：扩展 `looksLikeRowOrPlaceholder` → 新增 `looksLikeDescriptorNotName` 函数**

在 `looksLikeRowOrPlaceholder` 之后新增一个函数，用于识别「看起来像描述词而不是人名」的文本：

```javascript
function looksLikeDescriptorNotName(value) {
    // 身体特征描述词（高个子、矮个子、胖子等）
    // 学生角色/关系描述词
    return /^(高个子|矮个子|胖子|瘦子|近视|戴眼镜的?|个子高的?|个子矮的?|个头高的?|个头矮的?|身高高的?|身高矮的?|大个子|小个子|高的同学|矮的同学|身材高的?)$/.test(asText(value));
}
```

**改动 2：在 `splitNameList` 中过滤描述词**

在 `splitNameList` 末尾的 fallback 分支中，增加对描述词的过滤：

```diff
 return raw
     .split(/[、,，;；/和或与及\s]+/)
     .map(item => item.trim())
-    .filter(item => item && !looksLikeRowOrPlaceholder(item));
+    .filter(item => item && !looksLikeRowOrPlaceholder(item) && !looksLikeDescriptorNotName(item));
```

**改动 3：在 `parseSeatingConstraintsLocally` 的 `avoid_behind` 逻辑中增加前置检查**

在第 301 行正则捕获后、生成约束前，增加对捕获结果的过滤：

```diff
 const behind = extractRelatedAfter(sentence, /不想坐在(.+?)后面|不希望坐在(.+?)后面/, context);
+// 过滤掉非人名的描述词（如"高个子"、"矮个子"）
+const validBehind = behind.filter(name => !looksLikeDescriptorNotName(name));
 for (const target of targets) {
-    for (const related of behind.filter(name => name !== target)) {
+    for (const related of validBehind.filter(name => name !== target)) {
         addConstraint(constraints, { type: 'avoid_behind', target, related, reason: '不想坐在其后面', priority: 'hard' });
     }
 }
```

> [!TIP]
> 当「不想坐在高个子后面」被识别到但 related 被过滤为空时，可以考虑降级为一个 `prefer_front_row`（soft）约束，因为用户的本意是"不想被挡住视线"，等价于"希望坐前排"。这是一个可选的增强。

**改动 4：优化 AI 解析器的 system prompt**

在 `buildConstraintParseMessages` 的 system prompt 中增加一条规则：

```diff
 多人列表必须拆成多条约束；如果是"甲、乙、丙不希望坐第一排"，三人各一条。`;
+注意：如果学生说"不想坐在高个子后面"、"不想坐在矮个子旁边"等，这里的"高个子""矮个子"不是人名，是描述词。
+此类需求应理解为对座位区域的偏好（如希望坐前排以避免遮挡），而非指向具体某位同学。
+不要将"高个子""矮个子""胖子""瘦子""近视的""戴眼镜的"等身体特征描述词作为 related 字段的值。`;
```

**改动 5：在 `normalizeConstraintItems`（`mapAiConstraint`）中增加防御**

即使 AI 仍然返回了 `related: "高个子"`，在 `mapAiConstraint` 中也应拦截：

```diff
 const relatedName = splitNameList(related, context)[0];
+// 额外防御：即使通过了 splitNameList，仍然检查是否为描述词
+if (relatedName && looksLikeDescriptorNotName(relatedName)) {
+    // 描述词不是人名，不产生 avoid_behind 等约束
+    return [];
+}
```

---

#### [MODIFY] [seating-constraints-parser.test.js](file:///d:/607document/ICeCream/test/seating-constraints-parser.test.js)

新增测试用例：

```javascript
test('local parser does NOT treat "高个子" as a person name in avoid_behind constraint', () => {
    const constraints = parseSeatingConstraintsLocally({
        text: '王妍兰晓希望坐前排，不想坐在高个子后面。',
        students: [{ name: '王妍兰晓' }, { name: '高个子' }].concat(exampleStudents),
        // 注意：即使学生名单中不存在"高个子"也不应生成约束
    });

    // 不应产生 related="高个子" 的 avoid_behind 约束
    assert.ok(!constraints.some(item =>
        item.type === 'avoid_behind' && item.related === '高个子'
    ), '不应将"高个子"识别为人名');

    // 但"希望坐前排"仍然应被正确解析
    assert.ok(constraints.some(item =>
        item.type === 'front_row' && item.target === '王妍兰晓'
    ), '应保留"希望坐前排"约束');
});

test('AI normalization rejects descriptors like "高个子" as related names', () => {
    const constraints = normalizeConstraintItems([
        { type: 'avoid_behind', target: '王妍兰晓', related: '高个子', reason: '不想坐在高个子后面', priority: 'hard' },
    ], { students: exampleStudents });

    assert.ok(!constraints.some(item =>
        item.type === 'avoid_behind' && item.related === '高个子'
    ));
});
```

> [!IMPORTANT]
> 需要决定：当花名册中**恰好有一个学生叫"高个子"**时该怎么办？建议的策略是：
> - 如果 `context.students` 名单中包含该名字 → **仍然视为人名**，允许生成约束（`findRosterNames` 会命中）
> - 如果名单中不包含 → 视为描述词，过滤掉
> 
> 当前设计中 `splitNameList` 已经优先走 `findRosterNames` 路径（第 62-63 行），所以花名册中存在同名学生时不受影响。只需要确保 `looksLikeDescriptorNotName` 的检查发生在**花名册匹配之后**，即放在 fallback 分支或最终过滤处。

---

### 组件二：桌面图标布局调整（右下角垂直排列）

#### [MODIFY] [seating-planner.css](file:///d:/607document/ICeCream/public/css/seating-planner.css)

修改 `.sp-desk-items` 容器样式（第 1635-1645 行）：

```diff
 .sp-desk-items {
     position: absolute;
-    bottom: 8px;
-    left: 6px;
-    right: 6px;
+    bottom: 4px;
+    right: 4px;
     display: flex;
-    gap: 6px;
-    justify-content: space-between;
-    align-items: flex-end;
+    flex-direction: column;
+    gap: 2px;
+    align-items: flex-end;
     pointer-events: none;
 }
```

修改 `.sp-desk-item--books` 样式（第 1655-1658 行），移除 `margin-left: auto` — 因为改为垂直排列后不再需要：

```diff
 .sp-desk-item--books {
     order: 4;
-    margin-left: auto;
 }
```

> [!TIP]
> 由于改为垂直排列，多个图标会从右下角向上堆叠。现有 `order` 属性仍然有效（flex-direction: column 时 order 控制上下顺序）。建议调整 order 值使其从上到下依次为：📚书本(4) → ⚠️警告(3) → 👓眼镜(1) → 🍬糖果(0)，即重要的在上、装饰的在下。

可选微调：缩小图标字号以适应垂直布局

```diff
 .sp-desk-item {
-    font-size: 14px;
+    font-size: 12px;
     filter: drop-shadow(1px 1px 1px rgba(0, 0, 0, 0.2));
     transition: transform 0.2s ease;
 }
```

---

## 验证计划

### 自动化测试

```bash
cd d:\607document\ICeCream
node --test test/seating-constraints-parser.test.js
```

确认新增的 2 个测试用例通过，原有 3 个测试不受影响。

### 手动验证

1. **NLP 验证**：在学生需求输入框中输入 `王妍兰晓希望坐前排，不想坐在高个子后面。`，点击「提取需求」，检查约束列表中：
   - ✅ 有 `front_row` 约束指向「王妍兰晓」
   - ❌ 没有 `avoid_behind` 约束（related=高个子）

2. **图标布局验证**：生成座位表后，观察包含多个图标的课桌：
   - 图标应集中在课桌右下角
   - 多个图标上下垂直排列
   - 不影响课桌名牌和成绩/身高元数据的显示
