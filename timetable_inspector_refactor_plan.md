# 排课审查整体改版计划

## 目标

- 让 `排课审查` 从“很多重复黄卡片”变成一个清晰、紧凑、可定位的审查工具。
- 保留已有能力：悬浮窗、拖动、展开更多、点击定位、定位后不跳动。
- 不改求解算法、不改诊断/发布校验数据结构、不改后端接口；第一阶段只做前端信息组织、交互和样式。

## 当前问题

- 重复问题按单条大卡片展示，例如 `班级课表接近满载` 出现很多次，阅读成本高。
- 每条卡片里重复显示标题、说明、建议和 `定位`，导致长度过长、面板滚动压力大。
- 黄色背景和粗重字体用得太多，普通复核项看起来像强错误。
- `定位` 文字占空间，和卡片内容抢视觉焦点。
- `诊断 / 质量 / 发布`、`必须处理 / 建议复核 / 系统详情` 的层级不够明确，用户不容易判断先看哪里。
- `当前课节`、系统详情和问题列表混在同一个滚动区域时，容易让用户感觉位置在变。

## 目标信息架构

### 1. 悬浮窗外壳

- 保留桌面端可拖动悬浮窗。
- 保留移动端抽屉/流式展示。
- 顶部只保留必要信息：
  - 标题：`排课审查`
  - 总体状态：例如 `需处理 3 · 复核 340`
  - 折叠/展开按钮
- 不在顶部堆太多解释文字。

### 2. 主区域分三层

- `必须处理`：会阻止发布或明显错误的问题，默认展开。
- `建议复核`：质量建议、满载提醒、可发布但建议确认的问题，默认展开但使用紧凑列表。
- `系统详情`：诊断、质量、发布的底层明细，默认收起。

### 3. 重复问题按组展示

把同类问题从：

```text
八年级G8-10班
班级课表接近满载。
建议发布前复核
定位

八年级G8-11班
班级课表接近满载。
建议发布前复核
定位
```

改成：

```text
班级课表接近满载 340
建议发布前复核

八年级G8-10班
八年级G8-11班
八年级G8-12班
已显示 5/340 · 展开更多 20
```

整行可点击定位，不再每行常驻显示大号 `定位` 文案。

## 视觉方案

### 1. 字体

- 使用应用现有字体和字号，不单独引入新的字体风格。
- 问题组标题：13-14px，中等字重。
- 对象行：13px，常规或中等字重。
- 说明文字：12px，灰色，最多一行或两行。
- 避免大量粗体，只有组标题和关键对象名稍微强调。

### 2. 颜色

- `必须处理` 可以保留轻微红色/橙色提示。
- `建议复核` 不再整卡大面积黄色背景，改成：
  - 白底或浅灰底
  - 细边框
  - 左侧 2px 轻提示线
- 只有真正阻塞的问题才使用明显警示色。

### 3. 行高和密度

- 问题组使用小卡片，但内部对象使用列表行。
- 对象行高度控制在 32-40px。
- 行与行之间用分割线，不用一张张大卡片。
- 默认每组显示 5 条，展开后每次增加 20 条。

### 4. 定位入口

- 整行可点击。
- 常态不显示长 `定位` 文案，减少占宽。
- hover/focus 时右侧显示轻量定位提示，可以是小图标或很短的 `定位` 小字。
- 不能定位的问题保持普通文本行，不给 hover 定位反馈。

## 交互方案

### 1. 点击定位

- 点击对象行后切换到对应班级、教师或课节。
- 课表区域滚动和高亮目标。
- 排课审查悬浮窗保持原滚动位置。
- 刚点击的行保留短暂柔和高亮，用户能知道刚才点的是哪条。

### 2. 展开更多

- 每个问题组独立展开。
- 展开状态不因点击定位而丢失。
- 操作文案保持短：
  - `已显示 5/340`
  - `展开 20`
  - `收起`

### 3. 当前课节

- `当前课节` 不再插到问题列表上方把列表顶走。
- 建议改为悬浮窗底部的小型上下文块，或放入单独折叠区。
- 默认不抢主问题列表的阅读位置。

### 4. 系统详情

- `诊断 / 质量 / 发布` 底层详情继续保留，但默认收起。
- 系统详情里的长列表也使用同一套“分组 + 展开更多”规则。
- 不把系统内部指标和用户要处理的问题混在同一层。

## 实施阶段

### Phase 0：基线确认

- 截图确认当前排课审查的主要拥挤场景。
- 找到当前渲染入口：
  - `public/js/tools/timetable/view.js`
  - `public/css/timetable-planner.css`
  - `public/js/tools/timetable/controller.js`
  - `public/js/tools/timetable/grid-interactions.js`
  - `test/timetable-planner-ui.test.js`
- 固定当前已实现的能力：拖动、展开更多、点击定位、不跳动。

### Phase 1：快速视觉瘦身

- 去掉建议复核项的大面积黄色背景。
- 缩小对象行字体和行高。
- 将常驻 `定位` 文案改为 hover/focus 提示。
- 保留现有数据结构和列表结构，先让视觉不再“又长又重”。

### Phase 2：重复问题分组

- 新增前端分组 helper，按 `source + severity + title/message/category/type` 聚合同类问题。
- 组头展示问题类型、数量和一次性说明。
- 组内展示目标对象列表。
- 保留每个对象行的 `data-inspector-issue-key` 和定位数据。
- 展开更多从“每个来源组”细化到“每个问题类型组”。

### Phase 3：审查项定位体验收口

- 确认分组后的对象行仍能点击定位。
- 点击定位不清空展开状态。
- 点击定位不改变审查窗滚动位置。
- 刚点击的对象行短暂高亮。
- `当前课节` 改为不挤压问题列表的布局。

### Phase 4：系统详情降噪

- `系统详情` 默认收起。
- 诊断、质量、发布明细统一使用紧凑分组列表。
- 保留必要统计，不重复展示低价值说明。

### Phase 5：移动端和可访问性

- 移动端不使用自由拖动位置。
- 对象行可点击区域保持足够大。
- 可定位行有明确 `aria-label`。
- 键盘 focus 样式可见，但不夸张。

### Phase 6：测试和验收

- UI 渲染测试：
  - 重复复核项被分组，不再渲染大量重复大卡片。
  - 组头显示数量。
  - 组内对象行保留定位数据。
  - 不可定位项不显示定位交互。
- 交互测试：
  - 展开更多只影响当前问题组。
  - 点击对象行能定位到班级/教师/课节。
  - 点击后排课审查不跳回顶部。
  - 展开状态不丢失。
- 样式测试：
  - 建议复核项不再使用大面积黄色卡片。
  - 对象行采用紧凑行高。
  - hover/focus 反馈存在。
- 浏览器验收：
  - 打开有大量复核项的排课结果。
  - 面板默认可快速判断问题类型。
  - 点击某个班级问题能定位，回来仍看得到刚才点击的行。

## 建议回归命令

```bash
node --test test/timetable-planner-ui.test.js --test-name-pattern "inspector|locate"
node --test test/timetable-planner-ui.test.js --test-name-pattern "publish|solver|quality|diagnostic"
npm test
```

如果 Phase 2 之后视觉变化较大，再补一次浏览器冒烟：

```bash
npm run test:timetable:ui-smoke
```

## 验收标准

- 普通复核项不再像错误警告一样刺眼。
- 大量同类问题以“问题组 + 对象行”展示，不再重复铺满大卡片。
- 面板里能先看问题类型，再点具体对象。
- 点击定位后课表会动，排课审查本身不乱跳。
- 字体、行高、颜色和现有校务工具风格一致。
- 不改变求解、发布校验和诊断结果本身。

## 暂不做

- 不做新的后端诊断接口。
- 不做独立问题详情页。
- 不做自动修复建议重写。
- 不改求解算法。
- 不把审查问题持久化到项目数据里。

## 收口记录

- 动态标题栏已补齐：`排课审查` 标题旁按当前 view model 显示 `需处理 N · 复核 M`、`复核 N`、`可发布` 或 `未生成`，不再使用静态 `诊断 / 质量 / 发布` 文案。
- 浏览器 smoke 已覆盖大量复核项场景：临时种入 24 条同类复核项，验证标题栏数量、问题组数量、默认 5 条、展开后 24 条、点击定位后来源行高亮且审查面板不回到顶部。
- 最终验证已通过：
  - `node --test test/timetable-planner-ui.test.js --test-name-pattern "inspector|locate"`
  - `node --test test/timetable-planner-ui.test.js --test-name-pattern "publish|solver|quality|diagnostic"`
  - `npm run test:timetable:ui-smoke`
  - `npm test`
