"""Runtime Manim agent package.

The package keeps the agent action space explicit: plan, load skills, code,
critique, repair, and render. Routes expose this as /agent/run and
/agent/stream from the existing Manim FastAPI app.
"""

