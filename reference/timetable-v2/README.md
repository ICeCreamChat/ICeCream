# Timetable V2 Reference

This directory keeps the former `timetable-v2` implementation as non-runtime reference code.

- Source snapshot: `13cd9e9` (`fix(timetable-v2): preserve revision when saving imports`)
- Runtime status: not mounted by Gateway and not loaded by the classroom toolbox
- Removal: safe to delete after the legacy timetable restoration no longer needs V2 for comparison
