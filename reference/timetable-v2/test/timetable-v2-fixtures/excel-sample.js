/**
 * Excel/CSV 导入器测试样本。
 *
 * 含：
 *   - 正常行（多班级 / 多课程 / 多教师）
 *   - 一个连堂行（blockPreference=双/连堂）
 *   - 一个坏行（缺课程列 + 一个完全空行）
 *   - 一个多教师行（、分隔）
 *   - 同 (班级,课程,教师) 多行（验证课时累加聚合）
 *
 * 既提供「带表头的纯文本」也提供「行数组」两种入参形态。
 */

/** 带表头的逗号分隔纯文本。 */
export function excelSampleText() {
    return [
        '年级,班级,课程,教师,周课时,连堂,教室',
        '高一,1班,语文,张老师,2,单节,101',
        '高一,1班,语文,张老师,2,单节,101', // 同组第二行 → 累加到 4
        '高一,1班,数学,李老师,4,连堂,101', // 连堂行
        '高一,2班,体育,王老师/赵老师,2,单节,操场', // 多教师行
        '', // 空行（坏行）
        '高一,2班,,周老师,2,单节,102', // 缺课程（坏行）
    ].join('\n');
}

/** 已切分的二维行数组（含表头行）。 */
export function excelSampleRows() {
    return [
        ['年级', '班级', '课程', '教师', '周课时', '连堂', '教室'],
        ['高一', '1班', '语文', '张老师', '2', '单节', '101'],
        ['高一', '1班', '语文', '张老师', '2', '单节', '101'],
        ['高一', '1班', '数学', '李老师', '4', '连堂', '101'],
        ['高一', '2班', '体育', '王老师、赵老师', '2', '单节', '操场'],
        [],
        ['高一', '2班', '', '周老师', '2', '单节', '102'],
    ];
}

/** 无表头（走默认表头兜底）的行数组。 */
export function excelSampleNoHeaderRows() {
    return [
        ['高一', '1班', '语文', '张老师', '2', '单节', '101'],
        ['高一', '1班', '数学', '李老师', '4', '连堂', '101'],
    ];
}
